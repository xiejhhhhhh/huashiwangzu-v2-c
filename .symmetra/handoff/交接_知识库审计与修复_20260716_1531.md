# 交接文档：知识库全链路审计与修复（2026-07-16 15:31）

> 接手同事：读这一份即可续接。工作目录 `/Users/hekunhua/Documents/Agent/PHP/华世王镞_v2`，owner_id=4，PostgreSQL（库名 `华世王镞_v2`，5432）。
> 完整审计发现在 `开发文档/临时文档/01_知识库全链路审计与修复路线.md`；本地记忆在 `~/.claude/projects/-Users-hekunhua-Documents-Agent-PHP------v2/memory/`。

## 一、这次会话干了什么（已完成）

1. **全链路审计**：6 个并行子代理 + 2 份友军模型审计报告（GPT5.5/dp4，在 `开发文档/审计报告_*.md`）+ 主控亲自 SQL/日志核实。三方交叉验证。
2. **实测确诊数据洗不干净的性质**（华哥"删了重传"思路的更优替代）：
   - 现象：pptx 94%/ppt 75%/pdf 37%/docx 20% 缺语义层（graph 层），合计约 760 文档只有原始文本层。
   - **实测结论：不是 fusion/graph 逻辑 bug**。对卡住的文档 9084/9090 用 `enqueue_pipeline_stage_batch(fusion)` + `enqueue_incomplete_documents` 补投，完整跑通到 graph done、relation done（fusion 41 秒、完整 90 秒）。
   - **根因（主控读代码坐实，子代理暴毙）**：`pipeline_service.py:597-614` 续跑门槛 `_raw_complete` 要求"所有页的 page_render+raw_ocr+raw_vision 全 done"才投 fusion。pdf 几十页，历史上任一页某轮 raw 中断/漏跑 → `_raw_complete` 永远 False → fusion 永不触发 → 卡死。续跑 DAG 逻辑本身正确（fusion done 会无条件投 profile+graph，见 616-619）。**真 bug = raw 层页级任务中断后没有重投机制**。
3. **批量补齐进行中**：`enqueue_incomplete_documents` 投了约 600 文档进队列（从 source_validate 重跑，补齐缺失页 raw → 自动续 fusion→profile→graph）。worker 自控并发消化，系统健康（CPU 20%上下、内存 65%）。**这是纯数据修复，不改代码、不删库、不动原始数据**。
4. **文档+记忆整理（问题3）全部完成**：
   - 清垃圾（26 log/1 CSV/pyc/bak）、14 个临时文档归档到 `开发文档/开发历史记录/2026-07_语义治理与召回审计/`
   - 建审计文档 `开发文档/临时文档/01_知识库全链路审计与修复路线.md`
   - 本地记忆：更新"知识库语义治理-进展与待办"、把过时"续接快照-2026-06-23"瘦身重写为"环境与方向事实.md"（去污染）、重排 MEMORY.md
   - MCP 项目记忆：写入"知识库全链路审计与修复路线-2026-07-16"
   - 两份友军报告按华哥要求挪回 `开发文档/` 根目录（别归档）
5. **模型路由铁律钉进记忆**（华哥 2026-07-16 定死）：见下"关键约束"。

## 二、待办（华哥已授权：非模型问题直接全跑，异步子代理看着；审计报告问题顺手修，但先放子代理核实防假审计）

### A. 数据/运维清债（非模型，可直接全跑，用现成 admin 能力，先 dry_run 核实再 apply）
- **批量补齐剩余缺语义层文档**：继续 `knowledge.enqueue_incomplete_documents`（dry_run=false, limit 分批, priority 6）直到 `graph_status` 全 done。当前还剩 pdf ~410/docx ~156/pptx ~72/ppt ~41（在降）。
- **9038 条 kb_pipeline_runs running 记录债**：状态没收尾（reconcile 查任务队列 matched=0，说明队列本身干净，这是 pipeline_runs 表的陈旧记录）。需专门收尾逻辑或确认是否影响。
- **422 失败任务债**：`knowledge.classify_pipeline_debt` 分类 → `knowledge.apply_pipeline_debt`（archive_obsolete 不可恢复/retry_live 可重试）。340 kb_enterprise_import_file + 57 kb_pipeline_stage。
- **doc_profile_vectors 仅 28% 覆盖**（4328/15628）：补全（注意：向量化要调 Qwen3-8B，属模型，按华哥"可用本地/暂停"）。
- **实体确认闭环**：confirmed 仅 2242 vs candidate 8.98万（涉及 deepseek 归类，属模型）。

### B. 审计报告问题（先子代理核实真伪，确认真的再修）
- **profile_evolve 持续失败（dp4 报告）**：`modules/agent/backend/services/profile_evolve.py:312` 把 dict 直接赋给 text 列 `profile.profile_data = merged` → asyncpg DataError。修法：`json.dumps(merged, ensure_ascii=False)` 或列改 JSONB。**先核实是否真 bug（agent_user_profile 6 条是否真全 '{}'）**。
- **align_entity_batch 无 manifest/execution_contract（GPT5.5 报告）**：`modules/knowledge/backend/router.py:1385` runtime-only。修法：补 manifest public_actions + execution_contract（side_effect_level=admin, timeout>=1800, 声明写库风险）或改内部私有任务通道。
- **diagnose 库名误报**：诊断工具硬编码 `huashiwangzu_v2`，实际库名 `华世王镞_v2`。
- **model-router README 缺失**：docs_audit BLOCKER。

### C. 召回精准化（改核心检索代码，风险高，建议先子代理出方案主控审，别直接跑）
华哥痛点"召回要快+精准命中不枚举一堆"。`modules/knowledge/backend/services/search_service.py` + `router.py`：
- **无相关性分数阈值**，恒返 top_k=10（1415/2365）→ 低分噪音照返。加阈值砍低分。
- **rerank 硬编码关闭**（router.py:318 use_rerank=False）→ 有 bge-reranker-v2-m3 不用。打开接进主链路。
- **别名表下游没读**：`_match_query_entities` 只查正名，keyword/vector 通道零感知 → 昨天打齐成果没转化召回增益（别名覆盖率仅 2.8%）。
- **不按文档去重**（rrf dedupe_by_document 默认 False）→ 同文档多块刷屏 top_k。
- 快的底子好（HNSW+trgm 索引不暴力扫），不用动。

### D. 根治复发（改代码/架构）
- **raw 层页级任务中断无重投**（本次确诊的真 bug）：加 reconcile 或定期扫补投缺失 raw 子任务，让 `_raw_complete` 能被满足。或 `enqueue_incomplete_documents` 挂定时。
- **kb_char_slot_authority 预计算表无增量更新入口**（静态快照会过期）：查表 miss 时窄扫单坑位+UPSERT 落盘（懒加载自增长），别退全表扫。
- **打齐 align_entity_batch 同步跑占死 DB 连接**（三方铁证，单次 deepseek 87 秒撞 60 秒超时）：改后台任务，别同步 /api/modules/call。
- **agent 慢**：Qwen3-8B 向量保活消除冷启动；search 内联实体关系卡减往返。

### 模型类（华哥说可暂停/本地/deepseek，别死磕 GPT 额度）
- VLM 图片重识别（1.1万图，华哥曾说优先级>LLM）
- 实体归类垃圾桶剩余（deepseek 低并发）

## 三、关键约束（务必遵守）

### 🔴 模型路由铁律（华哥 2026-07-16 定死）
- **LLM 文本**：deepseek 可跑（**deepseek 只能 LLM，不能 VLM**）；GPT 也可；本地 gemma-4 兜底。
- **VLM 视觉**：**目前只有 GPT 能做**。GPT 额度不稳 → **不可用时降本地 qwen3-vl**（端口 30002，watchdog 懒加载）。**deepseek 绝不碰 VLM**。
- 当前 `backend/data/config/models.json` 降级链**已符合**：LLM=gpt-5.5-knowledge→deepseek-v4-flash；VLM=gpt-5.5-vision→qwen3-vl（本地）。有 pause_after_fallback + rate_limit_auto_pause（5分钟30次限流自动暂停）双保护。**这块已对，不用改**。

### 运行铁律
- 端口 33000。改完必 `kill -9 $(lsof -i :33000 -t)` + 清 __pycache__ + watchdog 自动拉起 + curl /api/health（status:ok/module_errors:null）+ 真调能力验。光 pytest 会被骗。
- 模块隔离：只动 `modules/{key}/`，跨模块走 call_capability。整数列 server_default 用 `sqlalchemy.text()` 非 `func.text()`。
- push 华哥授权自主（验过攒批推，GitHub 单线 main 干净，本地为准）。
- **子代理易暴毙**（本次 6 个里挂了 4 个，输出停在 153 字节初始占位）：派出后用 `stat -f '%z'` 查输出文件大小判断死活，暴毙就重派，别干等。
- **codegraph_callers 对动态注册的 pipeline stage 报"零调用者"是假象**，单一工具下结论必须 grep 交叉验证（本次误判"打齐没进管线"已纠正：graph.py:22 实调）。

## 四、华哥关于子代理的提醒（重要）
- 非模型问题：直接全跑，异步子代理看着。
- 审计报告问题：顺手修，但**先放子代理核实**——友军有时过度审计或给假审计，必须核实真伪再动手。
- 改核心检索/管线代码风险高：建议子代理出方案，主控审过再改，别直接跑崩线上。

## 五、当前运行态快照（交接时刻）
- 批量补齐在后台跑：缺语义层 pdf~410/docx~156/pptx~72/ppt~41（持续下降），kb_pipeline_stage 任务 completed 1554/pending 716/running 5，系统健康。
- 代码修复（B/C/D）**尚未开始**，只完成了诊断 + 数据补齐 + 文档记忆整理。
- 巡检子代理 semantic-governance-monitor 每 15 分钟看后台。
