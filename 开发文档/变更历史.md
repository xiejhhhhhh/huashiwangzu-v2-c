# 变更历史（里程碑 + 关键踩坑）

> 邮箱投件信完成后蒸馏到此，原信已删（代码可查 git log）。按主题分组，只记里程碑和值得记住的坑，不复述样板。

## 知识库五层多轮交叉印证 + 跨文件动态关联（2026-06-21，真实业务 PDF 验收通过）

按华哥 V1 真实设计落地（不是简化版"抽文本→分块→向量"）。全在 `modules/knowledge/`，视觉/嵌入/LLM 走框架网关。
- **第1层 原始多轮采集**（`raw_collection_service`/`pdf_render_service`，落 `kb_raw_data`，落盘后不可变）：第1轮文本提取 / 第2轮截图OCR（fitz渲染PNG→VLM）/ 第3轮视觉构成（VLM描述版面）。三轮独立证据分别存，防单一信息源偏差。
- **第4层 页级融合**（`fusion_service`，落 `kb_page_fusions`）：三轮 LLM 交叉印证 → 带 `融合正文/置信度/冲突JSON` 的单页权威描述。**对外检索默认用这层**。
- **第5层 文件画像**（`profile_service`，`kb_document_profiles`）：N页串联 → 主旨/摘要/章节/关键实体 + 画像向量(1024维bge-m3)。
- **第6层 实体图谱**（`entity_service`，从融合层重建）。
- **第7层 跨文件动态关联**（`relation_service`，`kb_file_relations`）：新文件增量与已有文件织网，画像向量余弦(0.6)+实体共现Jaccard(0.4)。华哥最看重。
- **全链路管道**（`pipeline_service`，`kb_pipeline`）：采集→融合→画像→图谱→关联，每步 commit，幂等可重入。

**派生层全部后台任务化 + 逐项提交**：原只有采集异步，融合/画像/关联/图谱都是同步端点→6页就 60s 超时、末尾才 commit→超时回滚数据全丢。改成入队 `kb_fuse`/`kb_profile`/`kb_relation`/`kb_graph`/`kb_pipeline`，由 `knowledge_worker` 消费；逐页/逐边 commit，中断只丢当前项、重跑跳过已完成。

**验收（真实微盘 PDF，华哥亲验路线 doc3红绷带/doc4圣痘士/doc5白富美）**：三份全链路 done，融合带置信度(0.87-0.94)+冲突记录，画像内容准确，图谱8节点8边，跨文件关联6条边互相织网；检索召回融合正文、Agent `knowledge:search` 闭环通；重启后 doc5 管道幂等续跑成功。pytest 81 passed。

**坑（真实数据验证才挖出，自测全过但功能崩）**：
- 报告自报"全链路通过"实为只用纯文本文件跑了简化老链路，五层灵魂一行真数据没产出。**纯文本文件无真图，第2/3轮 base64 把文本当 PNG 发给 VLM → 400**；真 PDF 渲染真 PNG VLM 正常。
- **派生层同步超时丢数据**：末尾一把梭 commit，超时杀连接→事务回滚→0行。改逐项 commit。
- **检索没接融合层**：`hybrid_search` 只查 `kb_chunks`，融合层(`kb_page_fusions`)无 embedding 列、走五层管道的文档检索 0 命中。修：融合落库后把"融合正文"切块+嵌入写进 `kb_chunks`(`index_fusions_to_chunks`)，检索主干不动即召回融合内容。
- `rebuild-graph` 端点遗漏未异步化(改派生层时漏改)→仍 60s 超时；补入队 `kb_graph`。

## 视觉模型网关接入（2026-06-20）

`gateway_router` + `model_services.describe_image` 接 mimo-v2.5（可看图，`api_key_env:MIMO_GATE1_KEY` 不硬编码，url/选型在 `models.json`）。实测 logo→真实中文 VLM 描述。知识库视觉轮、image-vision 模块都走它。

## 框架治理 G1–G13（2026-06-18 ~ 06-19）

地基审计结论：**地基健康，脏在填充层**。完成项：
- G3 框架占位清零 + 组件 `import.meta.glob` 自动扫描（删 26 个硬编码假占位 app）
- G4 抽公共文件操作层 composable；G5 回收站内嵌文件管理器 + 文件内拖放
- G6 补齐模型网关 HTTP 层（`backend/app/routers/gateway.py` 6 端点）；G7 补任务通知/日志端点；G9 任务队列 worker
- G11 文件去重收口；G12 跨模块调用契约（`module_registry` + `/api/modules/call`）；G12.1 能力注册带元数据（description/parameters/min_role）+ 技能清单接口；G13 接通 opencode-go 模型网关
- G8 端到端模块接入冒烟测试
- **坑**：跨模块禁互相 import / 禁直接读对方表，必须经框架通路（AGENTS 铁律 17-21）。

## Agent 模块（M0–M2 + 深度强化，2026-06-18 ~ 06-19）

从 V1 PHP 用 V2 架构完整重建：对话 + SSE 流式 + 消息入库、工具框架（技能发现器，读框架能力清单自动生成工具、走 `call_capability`）、三层提示词（系统/企业/个人画像，DB 存储 + 画像自动进化）、桌面感知、终端工具、UI 优化。
- **坑·流式收尾卡死**：`event_stream` 的 `yield [DONE]` 必须放在 `finally` 里，持久化/画像进化用独立 try 包住，否则收尾出错就发不出 `[DONE]` → 前端永久卡"停止"。前端 `done=true`(EOF) 也要 flush streamingText + 加 30s 空闲超时兜底。
- **坑·401**：前端 token key 统一 `v2_auth_token`；Agent 自己的 fetch 要加 401 自愈（清 token 跳登录），否则 token 过期卡死。后端鉴权正常，401 多为前端没带 token。
- **坑·端口**：主框架前端 `127.0.0.1:5173`、后端 `30004`（watchdog 守护）；agent 沙盒前端 `5180`（独立开发壳，有自己的登录态，非主框架）。两者别混。

## 模块群（2026-06-18 ~ 06-19）

- 6 个格式解析模块：pdf / docx / pptx / xlsx / text-parser / image-vision（均挂载，module_errors None）
- excel-engine 在线表格编辑器（后端挂载，11 端点；前端编辑待沙盒实测）
- desktop-tools（桌面/文件感知，查框架文件系统非宿主机）
- **terminal-tools（终端能力）**：工作区内执行命令。安全边界 = **macOS `sandbox-exec` 内核级沙盒**（只读系统 + 只读写工作区，越界/读宿主机/symlink穿透/open启GUI/写桌面全拒），非 macOS fail-closed。**决策：本地执行不用 Docker（C22），sandbox-exec 自带零下载。坑**：profile 工作区路径必须 `realpath`、用 `-p` 内联传防模型篡改、env 收最小白名单滤密钥。

## codemap 代码地图服务（2026-06-20，git afba241）

常驻内存索引全代码库（生产 310 文件，Python 用 ast、TS/Vue 用 tree-sitter），watchdog 热更新。6 能力 `get_file`/`impact`/`check_boundary`/`module_map`/`search`/`stats`，POST 文件路径 → 拿关联文件 + 影响面。Agent 改代码前查影响面用。
- **坑·前端空壳**：`indexer.py` 用 `os.path.dirname()` 返回 str 又当 Path 做 `/`，`str/str` 报错吞掉 → TS/Vue 相对 import 全解析失败。改 `Path(...).parent` + tree-sitter 后解决。
- **坑·路径**：查询入口要归一化（绝对路径转项目根相对再查），否则 agent 传绝对路径查不到。
- 排除 `sandbox/`/`tests/`/`脚本/`/`deploy/` 等开发壳（只索引生产代码）。

## D 系列：早期端到端 / 知识库 PDF 管线验证（2026-06 早期）

D11-D15：端到端总验、双 PDF 复验、cid 降权验证、Agent 工具调用审计字段、新视觉路验证。属早期知识库/PDF 管线调试，已验证。知识库正式迁入 V2 仍待做（见投递箱《项目书-知识库模块完整迁移》）。

## 后端稳定性修复（2026-06-18 ~ 06-20）

- **后端暴毙**根因：APP_DEBUG=True 致 SQLAlchemy echo 日志刷爆磁盘 + 无自动重启。修复：APP_DEBUG=False + `scripts/backend_watchdog.sh`（崩溃自动重启 + 日志超 50MB 归档）。
- **watchdog 抢端口**：多个 watchdog 实例互相杀抢 30004（曾 368 次 bind 失败 / 31 次重启）。修复：watchdog 加单实例 PID 锁。

## 2026-06-20 收口批次（韧性 + 安全 + codemap 全可用，git ff5bcec）

- **全项目深度审计**：10 维度审，挖出 2 高危（解析器跨用户读文件、`/api/modules/call` 绕 min_role）；codemap 边界引擎修白名单/表名误报（违规 32→0）；产出「模块可用框架公开能力清单」(01README)。
- **安全两高危修复+复核**：`call_capability` 执行侧加 min_role 校验、parser/excel/image-vision 读盘统一走 `check_file_access`。viewer 越权真实 HTTP 复打全 403/404；回归测试 `test_access_control_regressions`。坑：`list_capabilities` 按 role 过滤了但执行没卡（菜单挡了不检票）。
- **codemap 运行态挂载**：之前 HTTP 一直 404（相对导入框架加载器解析不了，"验收"全是 python 直驱）。修：registry.py 给每模块注册唯一包 `huashiwangzu_modules.{key}`，模块用干净相对导入。坑：sys.path+`from backend.X` 多模块撞名（codemap 挂上把 excel 撞挂）→ 命名空间隔离根治。
- **框架韧性**：模块按需自愈（请求触发重试 import，失败 503 带模块名/日志路径）、模块独立日志 `logs/modules/{module}.log`+请求ID、`--workers 3`、请求超时中间件（SSE 豁免）。坑：多 worker 内存状态不跨 worker → 共享状态（codemap 锁）必须文件持久化+原子写。
- **codemap 开发态增强**：stats 健康分(confidence/stale/freshness)、跨 worker 文件锁(locks.json)、rebuild、第三方库不索引定稿；用法写进 `_系统指令.md`。
- **审计遗留全清**：excel owner_id/类型、7 模块 README 重写、health 统一公开、desktop-tools 后台化、agent 迁移 except 细化、README 模块数校准。
- **Agent**：历史消息 timeline 真交错（思考↔工具按轮次，存有序时间线）；终端 `cd /` 越界命令层拦截。
- **端口/watchdog**：迁移固定 33000（被占只记日志不漂移，实际端口读 `.backend.port`）；watchdog 原子目录锁单实例、只杀自己 child。

## 验收教训（反复印证，写死）

**模块自测/单测全过 ≠ 功能对。** 多次出现"自评 ✅ 但真数据崩"：terminal-tools 漏逃逸、Agent 流式漏 `[DONE]`、codemap 前端空壳（24 单测没断言前端有边就漏过整个前端串联失效）。**验收必须亲手驱动真数据复核**，不信报告自述；同时别被自己的粗糙检查脚本误导（codemap 那次 13 个"漏"全是注释/资源误报，要追到根再下结论）。
