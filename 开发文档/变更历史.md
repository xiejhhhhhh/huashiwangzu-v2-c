# 变更历史

完整历史保存在 `开发文档/变更历史归档.md`。本文件只保留近期高信号记录和当前后续线索。

## 2026-06-23
### GPT 全项目审计 + 就地修复（2026-06-23）
- 做了什么：GPT-5 接手后首次全项目审计，五维度扫描（边界合规/框架干净/共性统一/流程逻辑/数据健康），就地修复 4 项。
- 改了哪些：15 个模块 manifest.json（min_role 补齐）、knowledge/router.py（logger）、office-gen/runtime/index.ts（模板同步）。
- 验收：health ok + knowledge:search 跨模块调用 ok + manifest 全部 explicit min_role + runtime 对齐，边界全在 modules/。
- 踩过的坑：无。
- 遗留问题：无。


### smoke_all 可信度修复 — 杀假绿/假红
- 改动: `dev_toolkit/smoke.py`
- 断言改判内层 success（`_cap_ok`），删除 `or status==200` 兜底
- 6 个原误报场景全修复（im send 建会话、office-gen 参数名、image-vision 用 file_id、excel-engine 真实 xlsx、docs-open 改 POST、A5 recycle 响应解析）
- 加 bge-m3 环境健康检查
- 验收：26 场景全绿；假绿复现旧判 GREEN 新判 RED ✓
- 限定范围：`git diff --name-only` → 仅 `dev_toolkit/smoke.py`，产品零改动

### 数据库迁移到 FlyEnv 集成环境
- 起因：重启后 EDB/homebrew/FlyEnv 三套 PG17 抢 5432。统一到 **FlyEnv 管理的 PG**（数据从 homebrew 克隆，版本同 17.10 同二进制，零丢失）。
- homebrew PG 设不自启、EDB `launchctl disable`，开机 FlyEnv 自动拉 PG。
- 坑：FlyEnv 手动 pg_ctl 启动要 `export LC_ALL=zh_CN.UTF-8` 否则 "postmaster became multithreaded"。全量备份 `~/Downloads/华世王镞_pg全量备份_20260623.sql`。

### 生图模块 · 多服务商模板架构（LiblibAI 接入）
- image-gen 重构为**服务商模板架构**（base.py 规范 + providers/{liblib,gptstore,placeholder} + image_templates.json，key 在 .env）。
- 接入 **LiblibAI 星流Star-3**（HMAC-SHA1 签名 + 异步轮询，实测出图）；中文 prompt 自动译英；成本记 `imagegen_records`。
- UI 图显示修复：前端改鉴权 fetch→blob→createObjectURL（原裸 `<img src>`+路径写反 → 裂图）。

### 全模块集测 + 前端 UI 集测
- 后端 25 模块 happy-path 真打活系统全绿；前端 Playwright（`frontend/tests/ui-e2e.spec.mjs`，36 测）27 应用无白窗、6 类文件开对查看器。
- 修复：xlsx 解析 3 bug（import ET/rid_map 双路径/router 未检查返回码，真测多 sheet parse 正常）、E4 storage_path 移除、knowledge 前端 liveProgressMap 空时回退文档 status。

### 项目工具台 MCP（dev_toolkit）
- 15 工具的 stdio MCP（brief/probe/call_capability/routes/capabilities/db_schema/tail_log/run_test/lint/code_explore/code_node/code_impact + memory_search/write/recent + smoke）。所有 agent 开工先连。
- 共享记忆存 `开发文档/项目记忆/` markdown + bge-m3 语义检索。修过：嵌入端口 8000→30000、中文降级分词、call_capability 传参 params→parameters。

### 测试工程化批（部分）+ E lint 回退（教训）
- A 测试效率：Playwright storageState + 条件等待（~30%，未达 60s）。B/C：测试铁律入 AGENTS.md + memory 规范。
- D：`dev_toolkit/smoke.py` 一键回归框架（断言只判 HTTP 200 有假绿、6 场景传错参有假红，**待修，见投递箱"修smoke可信度"**）。
- **E lint 回退**：ruff --fix 越界改治理9安全文件+modules、把 terminal-tools 改崩 → 全量回退。教训：lint 绝不全量 --fix，按模块手动+改完必验 health。保留 ruff.toml。

### 审计深度复核 · 维修批落盘验证 + 就地修复
- 产出：`收件箱/审计-深度复核(2026-06-23)/报告.md`
- **就修** D1 RecycleItem `created_at`/`updated_at` DB 缺列（`ALTER TABLE`）
- **就修** engine batch2 测试路径/模型名——33个测试全部通过（原10失败→0）
- **核实确认**：审计报告提到的 B1-B6/Y1-Y6 等流程问题此前已全部修复（SSE 增量 checkpoint、pipeline 短路、gather 异常、空文本过滤、向量上限等代码均已落地）
- **核实确认**：`__MODULE_OPEN_FILE_PAYLOAD__` 死类型已清理、`global.d.ts` 已干净
- pytest 186→197 passed / 17→5 failed（剩余5为测试环境特定问题：auth token 缺失×2、文件冲突×3）

### 项目工具台 MCP · 快测工具 + 跨 agent 共享记忆
- 产出: `收件箱/项目工具台MCP/审核报告.md`
- 新增 `dev_toolkit/` MCP 服务器, 9 个工具 (brief/probe/call_capability/tail_log/sql/web_read/memory_search/memory_write/memory_recent).
- 新建 `开发文档/项目记忆/` 目录作为跨 agent 共享记忆存储, 每条记忆一个 md 文件 + 自动维护索引 + 嵌入缓存.
- `AGENTS.md` 新增"开工铁律".
- 验证: probe→200, call_capability→image-gen 返回 3 模板, sql 只读/写拒绝, memory_write→真文件, memory_search 降级关键词.
- 踩坑: `/api/modules/call` 期望 `target_module` 字段而非 `module`, 已修并写入项目记忆.
- 遗留: bge-m3 未运行, 语义搜索降级关键词; trafilatura 未安装, web_read 降级 HTML 提取.

## 2026-06-23

### 生图模块 · 审计补修（产出物落盘 + 深度复核）
- 产出：`收件箱/生图模块-服务商模板适配器架构(LiblibAI实测通,自包含)/审计补修-2026-06-23.md`
- 审计原生图模块任务全部代码文件，确认架构落地完整、真数据验证通过
- 发现 P1 问题：README.md / 变更历史.md / AGENTS.md 修改未提交（产出物落盘缺失），已就地 `git commit fd59a9a`
- 踩坑：`AGENTS.md` 的"开工铁律"条目是 MCP 工具台任务产出，不应混入本模块任务提交范围

### 测试工程化大批(效率重构+规则统一+一键回归+lint卫生+生图修复)
- 产出: `收件箱/测试工程化大批/审核报告.md`
- A: Playwright 测试重构 — storageState 复用 + 条件等待 (36 测全过, 2.1 min)
- B: 测试铁律进 AGENTS.md (唯一定义, 四条铁律)
- C: memory_write 规范强调 + .gitignore 兜底
- D: smoke_all 一键回归 (dev_toolkit/smoke.py + server.py smoke_all tool)
- E: ruff lint 卫生 (ruff.toml + fix 93 自动 + 3 手动)
- F: image-gen 前端图显示修复 (auth fetch → blob → objectURL)
- **审计复核(2026-06-23 14:00)**: 补充修复 25 项(22 I001 + 3 E741 `l`→`line`), 新增根 `ruff.toml`, E701/E702 全局 ignore, lint **All checks passed**, 交付 `开发文档/项目记忆/测试工程化大批-审计复核.md`

### 生图模块 · 服务商模板适配器架构
- 产出：`收件箱/生图模块-服务商模板适配器架构(LiblibAI实测通,自包含)/审核报告.md`
- 改动：新增 6 文件 + 修改 7 文件
- 核心：image-gen 从"写死单一 gateway 调用"改成多服务商模板适配器架构
  - `providers/` — 适配器模式：base(契约) + liblib(HMAC-SHA1签名+轮询) + gptstore(封装旧gateway) + placeholder(PIL)
  - `image_templates.json` — 配置驱动(类 models.json)
  - `router.py` — 瘦调度层 + 中文自动翻译 + 成本记录到 `imagegen_records` 表
  - `frontend/index.vue` — 全新 UI：模板下拉 + 尺寸/数量 + 结果网格 + 积分显示
- 真数据验证：LiblibAI 星流Star-3 真实生图成功(15积分/张,余额1455),中文prompt自动译英
- 顺手修复：agent conversations 500(缺 conv_svc import)、knowledge current-user 404(路径错)
- 踩坑：`_Base.metadata.create_all(engine)` 对异步 engine 不生效，需 `conn.run_sync()`
- 踩坑：pydantic-settings 不把 .env 值注入 os.environ，模块需从 Settings 对象回退读取

### 收尾批 · 框架模块边界解耦 + 零散收口
- 产出：`收件箱/收尾批-边界解耦/审核报告.md`
- 改了 12 个文件（2 新增 + 10 修改），解决 3 个真违规 + 零散收口。
- 关键修法：
  - **事件解耦**：`file_transfer.py` 删除 `call_capability("knowledge","ingest")` 硬编码 → `emit_module_event("file.uploaded")`；knowledge 通过事件 handler 订阅，保持`knowledge:ingest` capability 供显式调用。
  - **跨模块读表**：`agent/admin.py` 删除直读 `agent_memory`/`memory_links`/`agent_experiences` → `memory:overview_stats` capability（`min_role="admin"`）。
  - **component_key 收口**：background-service 或无前端组件的模块 `component_key=""`。
  - **边界测试**：新增 `test_module_boundary_contracts.py`（7 断言）+ 扩充 `test_app_manifest_loading.py`（3 新用例）。
  - **零散**：docs-open manifest `route_prefix` 补上；image-gen 超时友好降级。
- 真数据验证：上传 txt → `success:true`，日志 auto-enqueued kb_pipeline；admin overview → memory/experience 统计数字对、无 500。
- 遗留：4 个 pytest 失败为前期已有（文件上传 409）。
- 踩坑：`from sqlalchemy import func as sa_func` 未使用但无害，保留不删。

### 修复批2 · 架构治理大批（框架净化+日志统一+runtime补齐+权限对齐+合规清理）
- 产出：`收件箱/修复批2-架构治理/审核报告.md`
- 改了 86 个文件，完成 6 个维度的架构治理。
- 关键修法：
  - **框架净化**：3 张 `framework_agent_*` 表 RENAME TO `agent_*`（保数据）；模型/CRUD/前端全线迁到 `modules/agent/`；框架再无 agent 业务残留
  - **check_file_access**：3 处越权路径统一改用框架 `check_file_access`（含分享文件兼容）
  - **日志统一**：48 文件统一 `v2.{key}.getChild("sub")` 规范；修复正则支持多级名；删 3 个陈旧日志
  - **manifest min_role**：6 处与 `register_capability` 不一致的 manifest 对齐
  - **runtime**：补齐 web-tools、升级 5 存根、修复 hello-world/docs-open
  - **合规**：200 假失败→`raise ValidationError`；框架文档 router 数更正
- 踩坑：子代理缺 `write` 权限导致任务中途失败，已修复所有子代理配置
- 遗留：4 个 pytest 失败是测试环境文件上传 409，非本批导致
- 产出：`收件箱/修复批1-全部功能Bug/审核报告.md`
- 改了 10 个模块文件 + 2 个 manifest,一次性修 13 bug。
- 关键修法：
  - B1: hardcoded `"pdf"` → 真实 `ext`,非PDF按类型定采集轮次
  - Y3/Y6: gather 全失败标 failed + pipeline 短路
  - B2/B3: CancelledError 显式捕获 + GeneratorExit 保护 yield
  - B4: 增量 checkpoint 每轮持久化 events
  - B6: manifest creatable_formats 字符串→对象数组
  - F-01/F-02: HTTPException/success=False → raise NotFound
- 踩坑：`chat.py` finally 块内 `s2` 在 async with 外失效(原代码即有),移动 persist loop 到 with 内。
- 遗留：agent admin.py:293 HTTP 200 假失败归 Batch 2。

### 审计信：全项目功能Bug清查
- 产出：`收件箱/审计信-功能Bug清查/审计报告.md`
- 就地修：docs-open embed.py `\d` SyntaxWarning → rf 原始字符串
- 关键发现：agent chat 500（高）、全局异常无日志（中）

### 治理7：knowledge 图谱 Three.js 单实例
- 做了什么：修复 knowledge 3D 图谱因直接路径导入 Three.js 产生多实例 warning。
- 改了哪些：`modules/knowledge/frontend/graph3d/three-addons.js`、`frontend/vite.config.ts`。
- 验收：`npm run build` 通过；真实登录后 GraphEngine import/init 成功，entity-graph 返回 135 nodes / 156 edges，浏览器 console 无多实例 warning。
- 遗留问题：无。

### 文档蒸馏与 Markdown 清理
- 做了什么：项目文档从过程堆积转为入口 README + 模块 README + 归档历史三层。
- 改了哪些：`开发文档/README.md`、`modules/agent/README.md`、`modules/codemap/README.md`、`modules/excel-engine/README.md`、`modules/knowledge/README.md`、`开发文档/变更历史归档.md`。
- 踩过的坑：`变更历史.md` 非严格时间顺序，不能机械剪前 400 行；先完整归档再重写当前索引。
- 遗留问题：Hermes 参考设计需拆专项落地。

## 2026-06-22

### 治理6：剩余前端 any 与类型压制收口
- 做了什么：清理前端/模块 runtime 剩余 `any/as any/@ts-expect-error`，并补齐 knowledge 3D 图谱 Three.js 本地类型声明。
- 改了哪些：`frontend/src/desktop/shell/index.vue`、`modules/docs-open/*`、`modules/office-gen/runtime/index.ts`、`modules/knowledge/frontend/graph3d/*`、`excel-engine/address-util.ts`。
- 验收：全局前端/模块类型压制扫描为空；治理4/5 API 收口扫描仍为空；`npm run build` 通过；真实登录联调通过。
- 遗留问题：Three.js 多实例 warning 已转治理7。

### 治理5：模块前端 API 类型契约收紧
- 做了什么：治理4触碰过的模块前端 API helper 和组件调用侧清理 `any/as any`，改为明确接口或 `unknown` + 类型守卫。
- 改了哪些：`knowledge/frontend/api.ts`、`WorkspaceGraph.vue`、`excel-engine/frontend/components/api-service.ts`、viewer 组件、`AgentConfigPanel.vue` 等。
- 验收：范围内 `any/as any/@ts-ignore` 扫描为空；`npm run build` 通过；真实登录后 knowledge 和 agent admin 接口返回契约对齐。
- 遗留问题：无。

### 治理4：模块前端/runtime API 通路收口
- 做了什么：模块前端裸 `fetch`、手写 Bearer、散落 `{success,data,error}` 解包收口到 runtime/helper，并补齐 helper 401 处理。
- 改了哪些：`modules/*/runtime/index.ts`、viewer/agent/knowledge/excel/office-gen 的前端 API helper。
- 验收：组件裸 `fetch`、手写 token、散落 envelope 解包扫描均空；helper/runtime 401 命中 27 个文件；`npm run build` 通过。
- 遗留问题：真实登录联调曾受后端可用性影响，后续治理5/6已覆盖真实联调。

### 治理3：前端统一 API 客户端收口
- 做了什么：复验 `frontend/src/shared/api/index.ts` 统一客户端，确认 token 注入、`{success,data,error}` 解包、401/403 处理和真实登录联调。
- 改了哪些：`frontend/src/shared/api/`、`frontend/src/platform/stores/user.ts`、桌面壳和相关 composables。
- 验收：裸 `fetch` 仅剩 `keepaliveFetch` 集中封装；`axios.create` 仅一处；`npm run build` 通过；真实登录 `何焜华/123rgE123` 后 `/current-user` 返回解包用户对象。
- 遗留问题：模块侧 API 通路已转治理4。

### 安全审计 P0/P1/P2/P3
- 做了什么：修复 docs-open token 越权、embed 鉴权、terminal run_python 沙盒、编辑器写权限、内容寻址、成本落库、office-gen 存储去重等问题。
- 改了哪些：`modules/docs-open/`、`modules/terminal-tools/`、`backend/app/services/file_upload_service.py`、`modules/office-gen/`、`modules/agent/` 等。
- 踩过的坑：`check_file_access` 必须 fail-closed；框架文件读写不能绕过 owner/share；异步成本落库不能 fire-and-forget。
- 遗留问题：中文文件名规则当时留有决策项，后续按 AGENTS 规则收口。

### Agent 治理增强第一批/第二批
- 做了什么：模型网关归正、后台任务统一、成本配额治理、逐 Agent 配置台、敏感操作审批和 worker 启动回收。
- 改了哪些：`backend/app/`、`modules/agent/`、`modules/image-gen/`、`modules/memory/`。
- 验收：真实重启 health ok；任务队列、成本记录、管理面板、审批队列均通过联调；前端构建通过。
- 踩过的坑：SQLAlchemy async 下 `inspect(sync_conn)` 触发 MissingGreenlet；SSE finally 块缺 lazy import 导致 NameError。

### 全模块去重治理审计
- 做了什么：扫描 27 模块在认证、数据库、文件、模型网关、后台任务、前端 API、能力注册上的自造轮子。
- 发现：22 项重复/漂移问题，高优先级包括 codemap 免鉴权、memory/agent fire-and-forget、office-gen 自建存储。
- 已修：codemap HTTP 认证门控、4 模块 manifest 补 public_actions、knowledge/codemap manifest 同步、memory/scheduler token key 统一。
- 后续：持续遵守跨模块调用只走框架统一通路。

### docs-open 文档开放接口
- 做了什么：新增自托管文档开放接口，提供令牌三件套、REST 端点、可嵌入编辑器、JSON 中间层。
- 改了哪些：`modules/docs-open/`、自动生成 component key map。
- 验收：令牌签发/失效、打开+嵌入、iframe、JSON 读写、权限隔离、字段对齐、前端构建均通过。
- 踩过的坑：FastAPI 依赖工厂不能误写成 async def；embed Query import 曾被误删。

### Agent 上下文装配 engine 批次
- 做了什么：事件溯源、预算、记忆、经验、压缩、降级、粘滞检测、事件重放和 admin 面板逐批落地。
- 改了哪些：`modules/agent/engine/`、`modules/memory/`、`modules/agent/frontend/admin/`。
- 验收：事件投影、经验注入、压缩事件、fallback chain、stuck detector、admin overview/replay 均有真实数据验证。
- 遗留问题：继续将 Hermes approval/registry/scheduler 等成熟设计拆专项参考。

---
### 项目工具台增强 · 精度工具 + 规则去重 + agent 归因
- 产出: `收件箱/工具台增强/审核报告.md`
- 新增 6 个精度工具:
  - `code_explore`/`code_node`/`code_impact` — codegraph 直通, 查代码/影响面不用 grep
  - `lint` — ruff 静态检查 Python, 改完秒级抓错
  - `routes` — 从 openapi.json 查准端点(参数/方法/路径)
  - `capabilities` — 扫描 manifest.json 查准模块能力
  - `db_schema` — 查数据库表名+列类型
  - `run_test` — 跑单个测试不跑全局
- **规则去重**: `AGENTS.md` 作为唯一真相源, `通用约定.md` 重复规则改为引用(4条去重), 保留独有6条
- **agent 归因**: `memory_write` 新增 `agent` 参数写入 frontmatter; `brief()` 新增"最近活动"段(git log + 带 agent 的记忆); `AGENTS.md` 开工铁律更新为先连工具台 MCP 全流程
- 验收: 15 工具全真调通过, 边界合规(git diff 无产品代码), memory frontmatter 含 agent 字段
- 提交: `git commit` 本信

## 2026-06-21 及以前

### knowledge 五层管线与前端验收
- 做了什么：知识库完成原始多轮采集、页级融合、文件画像、实体图谱、跨文件动态关联和前端四视图。
- 验收：真实业务 PDF 验证通过；融合置信度、冲突记录、图谱节点边、跨文件关联和检索召回均闭环。
- 踩过的坑：纯文本文件不能伪装 PNG 发 VLM；派生层同步超时会丢事务；检索必须接融合层。

### 框架治理 G1-G13 与模块群建设
- 做了什么：框架占位清零、组件自动扫描、公共文件操作层、模型网关、任务队列、跨模块调用契约、模块能力注册、opencode-go 网关接入。
- 模块群：格式解析、excel-engine、desktop-tools、terminal-tools、codemap 等基础模块上线。
- 核心约束：模块间禁止互相 import 和直接读表，统一走 `/api/modules/call` 与能力注册表。

### 早期稳定性与 D 系列验证
- 做了什么：后端 watchdog、日志治理、端到端验证、双 PDF 复验、视觉路验证、cid 降权验证等早期稳定性工作。
- 归档：详见 `开发文档/变更历史归档.md`。
