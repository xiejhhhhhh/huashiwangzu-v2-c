---
name: "何焜华账号 Agent聊天运行态诊断-2026-06-30"
type: reference
tags: ["agent", "运行态诊断", "上下文压缩", "用户画像", "记忆", "github-search", "执行闭环"]
created: 2026-06-30
agent: codex
---

只读诊断（未改产品代码）。账号 user_id=4，admin，已启用；会话表中 1 个 active、29 个 soft-deleted。当前会话 101 有 28 个 user_msg、108 个 tool_call、108 个 tool_result、108 个 assistant_msg，423+ events；最新 snapshot 投影 245 条模型消息、约 93956 token，但 compression-chain 为空。根因：deepseek-v4-flash 的 context_budget=null，而 context_pipeline._compress_context 仅在 budget 非空时触发压缩；工具循环每轮重复携带长历史，最近单轮累计 prompt_tokens 约 12万至28万。画像已演化到 version 35，focus=129、habits=131，只有字面去重，没有语义合并/上限。owner 4 的 stable rules 有 3 条同主题抖音链接规则，全部 active 且 hit_count=0；memory_links=0、experiences=0、trajectory_records=0、workflow_recipes=0、profile_signals=0，说明学习闭环尚未真正接入主聊天。聊天中出现完成后未回读验收：用户纠正“桌面文件仍是短链”；最终 file_id=512 为长链版，旧 510/511 已软删除。github-search 新模块 live capability 已验证：search 与 search_code 均成功返回真实 GitHub 结果。建议优先级：P0 给云模型设 3-5万软预算并让 null 也受 SAFETY cap 触发 compaction，降低工具轮次重复上下文；P1 建立工具写操作 postcondition 回读验收；P1 画像语义归并+字段上限、stable rule 按主题 upsert，业务链接存结构化资产而非高优先级规则；P2 自动记录 trajectory/correction 并激活 workflow mining；P2 收口 github-search 后建设抖音素材流水线（文本提链→video/note 长链→按ID去重→下载→publish→结构化存档）。
