---
name: "铁律:升级信件发给华哥前必先加固(注入3类已知盲区防御)"
type: reference
tags: ["process", "信件", "加固", "feedback", "verification"]
created: 2026-06-25
agent: claude
---

华哥指示(2026-06-25): 升级信件03-13都是发现系统性bug前起草的, 没把教训写进去, 导致同一类bug连续在02/03/04出现(执行agent报"通过"但核心功能没真跑通)。**以后每封信在让华哥发出前, 先更新加固该信**, 别让执行agent重复踩坑。

加固模板(按该信scope裁剪注入):
1. 必读文档加 AGENTS.md「开工铁律第6条产物验证铁律」。
2. 加「已知盲区与必做自检」段, 按该信改动面挑相关的:
   - 改/调gateway → "别猜kwarg, chat()是profile_key不是model_profile, 先code_explore/读签名"(02 review栽过)
   - 新建DB持久化状态表 → "owner_id等NOT NULL列, insert路径必须设, 签名带owner_id从runtime传, 配套改单测, db_schema确认+真触发查行数>0"(02 stuck/04 budget栽过)
   - 后台/异步/hook产物 → "做完 log_errors(agent)必须干净 + sql查产物表行数>0, 别只看任务建了/表在"(03 review栽过)
3. 验收标准加3条硬检查: log_errors干净 / 新表真有行 / 产物真落库。
4. 加"回信必须包含"段: 让执行agent自己贴出 kwarg名/NOT NULL列+行数/log_errors输出/产物样例, 防自评误判。

工具支撑: dev_toolkit已有 log_errors/db_schema/code_explore/sql。05已按此加固完。关联 [[运行时重构系统性盲区-多个引擎状态搬db漏owner-id-嵌套函数缺global]]。
