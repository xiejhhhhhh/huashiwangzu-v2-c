---
name: "验收资产生命周期门禁并写入二期清偿执行信"
type: "task"
tags: [asset-lifecycle, release-gate, test-pollution, execution-letter]
agent: "codex"
created: "2026-07-04T09:32:21.922784+00:00"
---

# 改了什么

- 复核用户回报的资产生命周期总收口实现，确认当前 dirty 范围为 18 个文件，覆盖 backend/app、backend/tests、dev_toolkit、modules/knowledge 与项目记忆。
- 读取收口报告并抽查核心实现：ContentPackage lifecycle service、Knowledge lifecycle debt service、dev_toolkit asset lifecycle tools、release gate、recycle permanent delete 路径。
- 写入下一封目标模式执行信：`开发文档/项目记忆/执行信-资产债务安全清偿与删除事件全覆盖二期.md`。

# 验证了什么

- ruff lint 覆盖本轮核心 Python 文件，全部 All checks passed。
- `backend/tests/test_content_package_lifecycle.py`：2 passed。
- `modules/knowledge/sandbox/test_module.py`：15 passed。
- `/api/health`：200，status=ok。
- `knowledge:audit_lifecycle_debt` 可调用。
- `content:audit_lifecycle_debt` 可调用，当前 active_packages=212，source_unavailable=204，archived_by_lifecycle=0。
- `release_gate(skip_ui=true, mode=preflight)` 正确返回 BLOCKER，clean_release_ready=false，release_safe=false，阻塞项为 ContentPackage lifecycle debt 与 Test data pollution。

# 是否还有残留风险

- 当前只是机制落地，历史债务尚未真实清偿：Knowledge source_unavailable=168，ContentPackage source_unavailable=204，ContentPackage missing_current_version=2，active_test_files=4，recycled_test_files=306。
- 文件夹递归永久删除和 empty_trash 当前只覆盖顶层 file recycle item，子文件事件仍需二期补齐。
- desktop-tools 删除路径是否绕开框架事件通路仍需二期确认/补齐。
- dev_toolkit 测试污染 marker 中 `test-` 过宽，有误删真实用户文件风险，二期执行信已要求收紧。

# 关联 commit

- 未提交；当前为验收与写信。
