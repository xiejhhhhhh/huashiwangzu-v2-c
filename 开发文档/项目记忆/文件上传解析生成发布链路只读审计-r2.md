---
name: "文件上传解析生成发布链路只读审计 r2"
type: "task"
tags: [files, upload, content, knowledge, audit, r2, read-only]
agent: "codex-flow-audit-files-r2"
created: "2026-07-03T09:46:26.503989+00:00"
---

# 文件/上传/解析/生成/发布链路只读审计 r2

Agent: codex-flow-audit-files-r2
分支: codex/sweep-quality-r2

本次只读审计覆盖文件上传、分片上传、Content Package 解析/生成/发布、知识库文档与源文件生命周期、模块读取 file_id 的 check_file_access 通路、运行时目录 Git 污染。

关键结论:
- 未确认 P0 文件访问越权；抽查 parser、docs-open、excel-engine、desktop-tools、terminal-tools、knowledge PDF/raw 读取等路径，核心 file_id 读取基本先走 check_file_access 或 file_reader 统一通路。
- P1: backend/data 下运行时产物已被 Git 跟踪，且 .gitignore 未覆盖 .tmp_exports/.tmp_downloads/.chunked_uploads/runtime。
- P1: 知识库和 Content Package 存在大量 live 记录指向 missing/deleted 源文件。活库 SQL 显示 kb_documents live=1321，其中 missing file record=840、source_file_deleted=428、source_file_live=53；framework_content_packages live=1549，其中 missing source record=763、source_file_deleted=726、source_file_live=60。
- P1: ContentExportService.publish 接收 target_file_id 但没有使用，实际先 export 创建新 framework_file_items，再 create_artifact，和 replace/publish 到指定文件语义不一致。
- P1/P2: /api/files/upload 普通上传临时文件只在 upload_file_from_path 成功后清理，ValidationError/上传服务失败会留下 .tmp_uploads 文件；当前 .tmp_uploads 351 files / 4.2M。
- P2: knowledge rechunk 无 chunks 时 ApiResponse(data={"error": ...}) 外层 success true；content:get_file_content 在无解析包/无块时返回 success true + source none + blocks=[]。

验证方式:
- 使用 brief/plan_task/worktree_guard/routes/capabilities/db_schema/db_reverse_audit/code_explore/code_node/code_impact/tail_log/sql。
- 使用 git ls-files、find、du、nl 只读固定文件系统和 Git 跟踪证据。
- 未改代码，未 commit，未跑测试。

建议修复归属:
- framework/backend infra: runtime git ignore 与历史 tracked runtime 文件清理。
- backend files: 普通上传临时文件 finally 清理与失败路径测试。
- framework content/export/artifact: publish target_file_id 语义收口。
- knowledge + framework content lifecycle: 源文件删除/缺失时 list/get/package/block/version 是否隐藏、标 stale 或 fail closed 的统一策略。
- knowledge: rechunk 空结果假成功。
- framework content: get_file_content 空解析结果契约。
