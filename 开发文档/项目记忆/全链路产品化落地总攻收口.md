# 全链路产品化落地总攻收口

时间：2026-07-04
执行 agent：codex-full-productization-r1

## 任务口径

本轮按《执行信-全链路产品化落地总攻大信》执行，不只修 ReleaseGate blocker，而是同时推进 Knowledge、Agent、Content/Artifact/Parser IR、Desktop 体验、ReleaseGate/测试矩阵/文档五条主线。已使用 5 个 lane 审计并由主会话合并落地。

## 本轮落地

### Lane A：Knowledge 总闭环

- `knowledge:search` 与 `/api/knowledge/search` 返回结果补齐来源解释字段：`file_id/source_file_id/source_file/mime_type/extension/content_package_id/block_id/chunk_index/section/paragraph/source_module/source_type/retrieval_source/explain/context_data`。
- Knowledge 前端搜索卡片展示来源文件、段落、检索来源与 score，避免只给一段孤立文本。
- Knowledge README 补验收矩阵，覆盖导入、检索、引用、生命周期、权限、污染清理。

### Lane B：Agent 工作中枢

- Workflow 列表后端聚合 `step/tool/failure/artifact/verification/reference` 数量，前端卡片展示步骤、子代理、工具调用、失败、产物、引用计数。
- Evidence reference 类型补 `source_module`，引用卡片展示 `source_module / source_tool`，并增加“复制 ID”动作。
- Agent README 补 workflow、tool ledger、references、artifact、failure recovery、权限、UI 验收矩阵。

### Lane C：Content / Artifact / Parser IR

- `csv-parser` 输出 block type 从中文 `段落/表格` 收口为 IR 兼容的 `paragraph/table`，sandbox 增加英文类型断言。
- `image-vision` 的 metadata block type 收口为 `paragraph`，减少 Parser legacy block 与 Content IR 的漂移。
- 模块开发文档补 Parser/Content IR 最小契约和 ContentPackage 状态机说明。
- 新增/补齐 parser 与相关模块 README 验收矩阵，覆盖 35 个 manifest 模块。

### Lane D：Desktop 产品体验

- `app-loader` 不再把普通 app 的空 `component_key` 静默渲染为空组件；只有 `background-service` 可使用空占位。普通 app 配置错误会进入 `ComponentRegistrationError`，避免白窗假绿。
- `terminal-tools`、`web-tools` manifest 的 `component_key` 改为空串，符合 background-service 契约。

### Lane E：ReleaseGate / 测试矩阵 / 文档

- 补齐 35 个模块 README 验收矩阵，新增缺失 README：`browser-tools`、`csv-parser`、`github-search`、`markdown-parser`、`structured-parser`。
- 修复 ReleaseGate component_key blocker：当前 `Component key contracts` 为 PASS。
- 修复 full gate 自身测试污染问题：`smoke.py` 收尾接入 `test_data_pollution_cleanup`，将本轮 smoke 生成的明确 marker 测试文件、回收站记录、Knowledge 文档、ContentPackage 状态清理干净。
- `test_smoke_queue_gate.py` 增加 Z3 清理钩子验证。

## 验证结果

- Ruff：`dev_toolkit/smoke.py dev_toolkit/test_smoke_queue_gate.py dev_toolkit/asset_lifecycle_tools.py dev_toolkit/test_asset_lifecycle_tools.py dev_toolkit/test_release_gate.py` 通过。
- Ruff：Knowledge/Agent/Parser 相关 Python 改动通过。
- `dev_toolkit/test_smoke_queue_gate.py dev_toolkit/test_asset_lifecycle_tools.py dev_toolkit/test_release_gate.py`：48 passed, 1 skipped。
- `modules/csv-parser/sandbox/test_module.py`：通过。
- `modules/image-vision/sandbox/test_module.py`：通过。
- Agent workflow tests：18 passed。
- `backend/tests/test_content_artifact_publish.py`：8 passed。
- Release/toolkit tests：65 passed, 1 skipped。
- `npm --prefix frontend run build`：通过，仅有既有 chunk-size warning。
- `smoke_all(skip_ui=true)`：PASS_WITH_DEBT，29 passed / 0 failed / 1 skipped；Z3 清理 selected=9、deleted_file_rows=9、archived_docs=6、archived_packages=6、physical_errors=0。
- `test_data_pollution_audit`：active=0, recycled=0, knowledge=0, content=0。
- `release_gate(skip_ui=true, mode=preflight)`：PASS_WITH_DEBT，无 blocker。
- `release_gate(skip_ui=true, mode=full)`：PASS_WITH_DEBT，无 blocker；smoke 39s 通过但 UI skipped；sandbox 35 pass / 0 fail / 0 skip，19 个模块存在 chunk warning debt。

## 当前 ReleaseGate 结论

无 BLOCKER。当前可发布口径为 `PASS_WITH_DEBT`，`deploy_allowed=true`。

剩余 debt：

1. Git worktree dirty：本轮总攻改动尚未提交。
2. UI coverage：本轮 full gate 使用 `--skip-ui`，未跑完整 Playwright UI gate。
3. Smoke test：后端 smoke 通过，但 UI 场景跳过。
4. Sandbox matrix：35/35 pass，但 19 个模块存在 frontend sandbox chunk warning。

## 兼容问题与观察项

- full gate 期间 `image-vision/Knowledge raw collection` 触发过 mimo 401 与 qwen3-vl context 过大，系统按降级路径继续完成，未形成 gate blocker。下一轮如要做视觉解析质量，应单独处理模型 key 与图片压缩/上下文预算。
- Knowledge 当前 active docs 为 0，因此搜索字段形态和前端展示已补齐，但真实有文档数据时的“点击定位页/段落”还需要 UI e2e 继续加强。
- Agent failure recovery 的完整动作链（重试失败工具、继续执行、导出错误报告）本轮主要补了列表可观测与引用卡片，深度恢复仍是后续专项。

## 下一封收口信建议

1. 跑完整 UI gate：不要 `--skip-ui`，重点验 Desktop 启动器、Artifact/File 打开下载、Knowledge 引用卡片、Agent workflow 页面。
2. 处理 19 个 sandbox chunk warning：优先拆/瘦前端 sandbox 构建输入，减少矩阵 debt。
3. 做 Knowledge 有数据 e2e：上传文件 → ingest → search → Agent 引用 → 点击回源定位 → 删除/恢复 → release gate 污染为 0。
4. 做 Agent failure recovery 专项：失败工具重试、继续执行、取消/恢复、错误报告导出进入 Artifact/File。
5. 做视觉解析质量专项：修 mimo key 或禁用无效 provider，给 qwen3-vl 图片输入加尺寸/上下文预算保护。

## 关联提交

尚未提交。
