---
name: "后端语义失败统一与假绿归零收口"
type: "task"
tags: [backend, semantic-failure, false-green, task-worker, release-gate, smoke]
agent: "codex-backend-semantic-failure-r1"
created: "2026-07-04T11:17:00.540401+00:00"
---

# 改了什么

- 新增 `backend/app/services/module_registry.py::semantic_failure_reason(result)`，统一识别 `success:false`、顶层/内层 `error`、`status in failed/error`、legacy `code != 0`，并递归检查 `data` / `result` envelope。
- `/api/modules/call`、task worker、task queue audit、`/api/health` 复用统一口径；unknown `POST /api/tasks/submit` 先校验 handler，未知 `task_type` 直接 422，不入队。
- `dev_toolkit/smoke.py`、`release_gate.py`、`release_response.py` 收紧外层 200 假绿：任意 envelope 层语义失败都失败，release gate 缺 `SMOKE_JSON` fail-closed，release response 不再把有 debt/clean_pass:false 的 PASS 当 clean success。
- 新增/扩展后端和 dev_toolkit 回归测试；写入 `开发文档/项目记忆/后端语义失败统一与假绿归零收口.md`。

# 验证了什么

- 改动文件级 ruff 全部通过。
- `set -a; source backend/.env; set +a; backend/.venv/bin/python -m pytest backend/tests/test_task_queue_audit.py`：14 passed。
- `backend/.venv/bin/python -m pytest dev_toolkit/test_release_gate.py dev_toolkit/test_release_response.py`：27 passed, 1 skipped。
- 补充测试：module call false success、task worker semantics、tasks API permissions、framework health、smoke queue gate、release response 均通过。
- 活栈：`/api/health` OK；`/api/tasks/worker/audit` semantic completed failure count 0；`agent:update_system_prompt {}` 返回 422 + success:false；unknown task submit 返回 422 + success:false；codemap direct HTTP 与 capability 错误文本一致。
- `release_gate.py --skip-ui --preflight` 未假绿，机器 JSON 给出 BLOCKER，真实原因是已有 test data pollution active=9。

# 残留风险

- `backend/.venv/bin/ruff check backend/app dev_toolkit` 已运行但失败，原因是大量既有未改文件 lint 债（134 个，主要 import 排序/未用导入/E712）。本轮改动文件级 ruff 通过。
- 当前工作区存在并发/既有越界 dirty（frontend、modules/agent、backend/app/services/content 等），finish_task 因此边界检查失败；本轮语义失败实际改动文件均在执行信允许范围内。
- 未提交 commit。

# 关联 commit

- 未提交。
