---
name: "桌面窗口动画与鼠标磁吸分屏收口"
type: "task"
tags: [frontend, desktop-window, snap, playwright, window-manager]
agent: "codex-desktop-window-snap-r1"
created: "2026-07-04T11:41:31.058043+00:00"
---

完成《桌面窗口动画与鼠标磁吸分屏》执行信。改动集中在桌面窗口管理允许范围：desktop-window-frame 增加打开/关闭/最小化/恢复轻量动画、snap preview、无障碍语义和 reduced-motion；use-window-interaction 增加左/右/上边缘吸附、mouseup 释放坐标重算、极小工作区 clamp；window-manager 增加顶部吸附后的还原几何和按 zIndex 激活下一个可见窗口；window-snap-preview.spec 覆盖左/右/上预览、preview 几何、松手落位、顶部最大化后恢复。验证通过：cd frontend && npm run build；cd frontend && npx playwright test tests/window-snap-preview.spec.mjs --project=admin（3 passed）；目标范围 any/as any/@ts-ignore/@ts-expect-error 扫描无命中；git diff --check 通过；worktree_guard 基于开工脏文件基线确认没有新增越界/禁止路径。残留风险：专项 Playwright 是前端交互隔离测试，mock 桌面 API，不等同全活栈业务回归；当前 worktree 有大量其他并行任务脏文件，本任务未接管也未回退。关联 commit：未提交。
