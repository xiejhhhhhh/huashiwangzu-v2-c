# 执行信：测试污染回归清零与门禁稳态

## 目标

当前 release_gate 只剩 Test data pollution BLOCKER：active=9、knowledge=6、content=6。把这批新回归污染清零，并加防回归规则，确保后续测试不再污染真实文件/Knowledge/Content 域。

## 边界

允许：

```text
dev_toolkit/asset_lifecycle_tools.py
dev_toolkit/release_gate.py
dev_toolkit/test_asset_lifecycle_tools.py
dev_toolkit/test_release_gate.py
backend/tests/ 与本任务直接相关测试
开发文档/项目记忆/
```

禁止：

```text
frontend/
modules/agent/
backend/app/services/content/ 非必要不要碰
```

## 必做

1. 审计 active=9、knowledge=6、content=6 的具体 marker 样本。
2. 判断是测试执行新增污染，还是 marker 规则误判。
3. 如是真污染：使用/完善 `test_data_pollution_cleanup confirm="CLEAN_TEST_DATA"` 清零。
4. 如是误判：收紧 marker 或排除非污染样本，不能误删真实用户数据。
5. 给测试创建的数据加更明确 marker，并在测试结束永久清理。
6. release_gate(preflight, skip_ui=true) 不再因 Test data pollution BLOCKER。

## 验收

```text
test_data_pollution_audit: active=0, recycled=0, knowledge=0, content=0
release_gate(skip_ui=true, mode=preflight): 不再有 Test data pollution BLOCKER
/api/health ok
```

必跑：

```bash
backend/.venv/bin/ruff check dev_toolkit/asset_lifecycle_tools.py dev_toolkit/release_gate.py
backend/.venv/bin/python -m pytest dev_toolkit/test_asset_lifecycle_tools.py dev_toolkit/test_release_gate.py
```

## 交付

写：

```text
开发文档/项目记忆/测试污染回归清零与门禁稳态收口.md
```

并调用：

```text
finish_task(...)
memory_write(agent="codex-test-pollution-stability-r1")
mcp_feedback(agent="codex-test-pollution-stability-r1")
```

## 提示词

请读取并执行：‘/Users/hekunhua/Documents/Agent/PHP/华世王镞_v2/开发文档/项目记忆/执行信-测试污染回归清零与门禁稳态.md’
