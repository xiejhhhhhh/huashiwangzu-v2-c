### 文件系统重复文件冲突策略与自动改名收口（2026-06-26）

- **做了什么**：将文件系统的重复冲突处理收口成统一策略。上传新增 `on_conflict` 参数（raise/auto_rename/replace），409 时附带结构化冲突数据；`create_file` 自动改名从 `name 2.ext` 修复为 `name(1).ext`；不同用户同名文件天然并存（owner_id 过滤，无需改动）。
- **Agent**：executor
- **改了哪些**：`exceptions.py`、`handlers.py`、`file_service.py`、`file_upload_service.py`、`file_create_service.py`、`file_transfer.py`、`test_file_system_conflicts.py` 等 7 个文件
- **踩过的坑**：没有
- **遗留问题**：`test_create_folder_and_list` 用固定名 "test-folder-001" 会因上次运行留下的脏数据失败，需改为 uuid 命名
