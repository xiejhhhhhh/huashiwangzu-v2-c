---
name: "审计修复-P0-私有模块激活失败不得外层成功且必须回滚运行时路由"
type: task
tags: ["审计修复", "P0", "私有模块", "rollback", "MissingGreenlet", "false-success"]
created: 2026-07-03
agent: opencode
---

代理: opencode

修复了 2 个 P0 级 BUG:
1. Rollback 端点外层 `success=false` 缺失 — `api_rollback_private_module` 未检查 `result.status == "failed"`，500 下返回 `success=true`（同原 activate 端点已修但 rollback 遗漏）
2. `rollback_private_module` MissingGreenlet 异常 — `db.commit()` 后 `record` 已 expired，然后 except 块直接 `record.status = "failed"` 在 async 下触发 MissingGreenlet；补 `await db.refresh(record)` 保护

新增测试: `test_private_module_rollback_failure_exposes_success_false` — 破坏已激活模块的 router.py → rollback → 验证 `success=false`、`status=failed`、route 404

改动文件: backend/app/routers/private_modules.py, backend/app/services/private_module_service.py, backend/tests/test_private_modules_lifecycle.py

验证: pytest 3/3 passed, ruff all checks passed, health ok
