---
name: "测试污染回归清零与门禁稳态收口"
type: "task"
tags: [release-gate, test-pollution, asset-lifecycle, dev-toolkit]
agent: "codex-test-pollution-stability-r1"
created: "2026-07-04T12:12:34.579405+00:00"
---

# 测试污染回归清零与门禁稳态收口

## 目标

执行 `执行信-测试污染回归清零与门禁稳态.md`，把 release gate 中的 Test data pollution 阻塞项清零，并补防回归规则，确保后续测试数据不会污染真实 File / Knowledge / ContentPackage 域。

## 污染审计与判断

早前审计命中：

- `active_test_files=9`
- `recycled_test_files=0`
- `knowledge_documents_from_test_files=6`
- `content_packages_from_test_files=6`
- `uploads_test_artifacts=9`

样本为同一批测试 marker 文件：

- `634 smoke-1783163050603.xlsx`
- `633 smoke-1783163050603.docx`
- `632 smoke-xl-1783163050603.xlsx`
- `631 smoke-doc-1783163050603.txt`
- `630 smoke-img-1783163050603.png`
- `629 smoke-b2p-1783163050603.pdf`
- `628 smoke-b2-1783163050603.txt`
- `627 recycle-1783163050603.bin`
- `626 smoke-1783163050603.txt`

判断：这批是测试执行新增污染，不是 marker 误判。理由是文件名全部带 `smoke-` / `recycle-` 测试前缀，时间戳批次一致，并且关联 Knowledge 与 ContentPackage 记录。

## 清理结果

已通过 `test_data_pollution_cleanup(confirm="CLEAN_TEST_DATA")` 执行真实清理：

- `selected_files=9`
- `archived_documents=6`
- `archived_packages=6`
- `deleted_recycle_rows=0`
- `deleted_file_rows=9`
- `physical_deleted_files=9`
- `physical_delete_errors=[]`

最终复验：

- `test_data_pollution_audit(limit=20)`：`active=0, recycled=0, knowledge=0, content=0, uploads=0, candidate=0`
- `test_data_pollution_cleanup(dry_run=true)`：`selected=0, changed=0`

## 防回归改动

- `dev_toolkit/release_gate.py`
  - `Test data pollution` 改为只要 `active_test_files/recycled_test_files/knowledge_documents_from_test_files/content_packages_from_test_files` 任一非零，就判 `BLOCKER`。
  - 全零才判 `PASS`，不再把 recycled / knowledge / content 残留降级为 `DEBT`。
- `dev_toolkit/test_release_gate.py`
  - 更新污染分类测试：`active=0` 但 `recycled/knowledge/content > 0` 时必须是 `BLOCKER`。
  - 新增参数化回归测试，逐项覆盖 `active_test_files`、`recycled_test_files`、`knowledge_documents_from_test_files`、`content_packages_from_test_files` 任一非零时 release gate 必须阻断。
  - 补全全零状态下 `Test data pollution` 为 `PASS` 的断言。
- `dev_toolkit/test_asset_lifecycle_tools.py`
  - 新增 `_audit_sql` 字段覆盖断言，确保审计 SQL 明确输出 active / recycled / knowledge / content / uploads / candidate 计数字段。
  - 强化 cleanup SQL 断言，确保清理逻辑仍会归档 `kb_documents`、归档 `framework_content_packages`、删除 `framework_file_recycle_items` 与 `framework_file_items`。
  - 保留共享 `storage_path/md5_hash` 保护断言，避免误删真实用户共享物理文件。

## 最终验收

- `/api/health`
  - `success=true`
  - `status=ok`
  - `database=ok`
  - worker running
  - task queue clean
- `test_data_pollution_audit(limit=20)`
  - `active_test_files=0`
  - `recycled_test_files=0`
  - `knowledge_documents_from_test_files=0`
  - `content_packages_from_test_files=0`
  - `uploads_test_artifacts=0`
  - `candidate_file_count=0`
- `release_gate(skip_ui=true, mode=preflight)`
  - verdict: `PASS_WITH_DEBT`
  - no `BLOCKER`
  - `Test data pollution`: `PASS`
  - detail: `active=0, recycled=0, knowledge=0, content=0`
  - remaining debt only comes from dirty worktree, skipped UI, preflight smoke skip, sandbox preflight skip.
- `PYTHONDONTWRITEBYTECODE=1 backend/.venv/bin/python dev_toolkit/release_gate.py --skip-ui --preflight`
  - exit code `0`
  - verdict: `PASS_WITH_DEBT`
  - no `BLOCKER`
  - `Test data pollution`: `PASS active=0, recycled=0, knowledge=0, content=0`
- `backend/.venv/bin/ruff check dev_toolkit/asset_lifecycle_tools.py dev_toolkit/release_gate.py dev_toolkit/test_asset_lifecycle_tools.py dev_toolkit/test_release_gate.py`
  - 通过。
- `PYTHONDONTWRITEBYTECODE=1 backend/.venv/bin/python -m pytest -p no:cacheprovider dev_toolkit/test_asset_lifecycle_tools.py dev_toolkit/test_release_gate.py`
  - `32 passed, 1 skipped`。
- `finish_task`
  - 边界检查通过；本轮只确认 `dev_toolkit/release_gate.py`、`dev_toolkit/test_asset_lifecycle_tools.py`、`dev_toolkit/test_release_gate.py` 与本文档为任务改动。
  - 工具台再次运行定向 lint / pytest：通过。

## 边界说明

当前工作区有大量并发/既有脏改动，包含 ContentPackage、frontend、modules/agent、dev_toolkit 其它门禁文件等。本任务只新增/修改测试污染门禁相关文件：

- `dev_toolkit/release_gate.py`
- `dev_toolkit/test_asset_lifecycle_tools.py`
- `dev_toolkit/test_release_gate.py`
- `开发文档/项目记忆/测试污染回归清零与门禁稳态收口.md`

未修改 `frontend/`、`modules/agent/`、`backend/app/services/content/`。

## 关联 commit

未提交。
