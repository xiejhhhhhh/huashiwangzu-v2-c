---
name: "模块专项维修五包收敛"
type: "task"
tags: [modules, audit, parser-media, codemap, tools, excel, manifest, sandbox]
agent: "codex-module-audit-conductor"
created: "2026-07-03T18:27:13.864164+00:00"
---

# 改了什么
- A 包：Parser/Media 未发现 P0 file_id 越权，12 个目标模块均确认走 run_uploaded_file_capability -> read_uploaded_file -> check_file_access。
- B 包：codemap stats/rebuild DB 指标 enrich 失败不再静默吞错，返回 metrics_degraded/metrics_enrichment_error；knowledge /documents/chunk 重分块逻辑下沉到 services/chunk_rebuild_service.py。
- C 包：browser-tools 下载文件名路径注入拦截、close cleanup 返回 degraded/errors；docs-open base URL 不再信任 X-Forwarded-*，embed 插值改为 HTML/JSON escaping；github-search cache 写入加文件锁与唯一 tmp；terminal-tools run_python cleanup_error 显式回传。
- D 包：desktop-tools/office-gen 纯 helper 下沉 service；excel-engine CSV/XLSX 临时文件 cleanup 补齐并加 sandbox 覆盖。
- E 包：image-gen/memory/pptx-parser/scheduler manifest description 与 README 验收命令收敛，未改 action/parameters/min_role。

# 验证了什么
- A 包 12 个 parser/media sandbox 合计 46 passed。
- module_sandbox_matrix --json 全部 check: pass。
- scripts/check-capability-drift.py OK (172 registered public capabilities)。
- ruff check 改动 Python 文件 All checks passed。
- git diff --check -- modules 通过。
- browser/docs-open/github/terminal/codemap/knowledge/excel/desktop/office/image-gen/memory/pptx/scheduler 等关键 sandbox 均通过。

# 是否还有残留风险
- browser-tools session 仍是进程内状态；workers=3 下跨 worker session 持久化需要后续更大专项。
- 开工前和并行期间存在 backend/dev_toolkit/frontend/tests/开发文档 非模块 dirty，本轮未回退，模块维修产品改动限定在 modules/。

# 关联 commit
- 未提交。
