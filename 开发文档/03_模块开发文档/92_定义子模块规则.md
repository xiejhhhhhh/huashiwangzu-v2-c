# 定义子模块规则

## 目标

- 顶层模块可以拥有 `submodules/`。
- 子模块由父模块管理，不被桌面壳全局扫描。
- 子模块如果要成为桌面应用，必须升级为顶层模块。

## 验收

- 知识库可以拆 catalog、ingestion、retrieval、qa、graph、evaluation 等子模块。
- 桌面壳只看到 `modules/knowledge/manifest.json`，看不到内部子模块。

## 完成后归档

完成后把当前子模块规则合并回本目录 `README.md`，再删除本文件。
