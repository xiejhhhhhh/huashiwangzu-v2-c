# 桌面壳统一一致性 + 双皮肤底座方案

> 日期：2026-07-18
> 状态：**macOS 皮肤已收完；Win11 仅插槽，未做完整 chrome**
> 基线：`http://127.0.0.1:5173/desktop`
> 参考 A：[macOS 27](https://macos27.kimi.page/)
> 参考 B：[macOS 28 / fevze](https://macbook.fevze.com/)
> 原则：一套行为运行时 + 可切换皮肤；先完成 mac，再做 win。

---

## 0. 架构结论

```text
共享运行时：窗口 / Product / Content Open / 文件 / 权限
皮肤层：metrics + CSS 变量 + 壳组件投影
```

| 皮肤 | 状态 |
|------|------|
| `macos` | **完成**（一致性配方 + 几何 + 壁纸 + CC/toast + 业务入口 chrome） |
| `win11` | **仅 token/metrics 插槽**（完整 Taskbar/Start/控件未做） |

### 兼容接口

```js
window.__HSWZ_DESKTOP_SHELL__.getShellSkin()
window.__HSWZ_DESKTOP_SHELL__.setShellSkin('macos' | 'win11')
window.__HSWZ_DESKTOP_SHELL__.listShellSkins()
useDesktopConfig().setShellSkin('macos')
// persist: localStorage desktop.shellSkin
```

代码：`frontend/src/desktop/skins/*`、`config/desktop-preferences.ts`、`config/desktop-chrome-metrics.ts`

---

## 1. macOS 已完成清单

### 1.1 材质
- 单配方 `--desktop-lg-filter` ≈ `blur(34px) saturate(182%)`
- `.glass-*` primitive 统一消费变量
- 壳层/业务入口私有 blur 已收到变量（snap 预览 4px、`@supports` 降级除外）

### 1.2 几何（mac）
| 项 | 值 |
|---|---:|
| MenuBar | 28px |
| Dock | 66px |
| Dock 底距 | 12px |
| Dock 圆角 | 18px |
| 窗口圆角 | 14px |
| work bottom inset | 78px |

`getDesktopWorkArea()` 读 active skin。

### 1.3 壁纸
- 默认：`/desktop/wallpaper-macos-default.svg`（自有资源，非 demo 原图拷贝）

### 1.4 系统面板
- Control Center：22px 圆角 + 渐变玻璃面板
- Toast：底中堆叠
- 通知中心外壳 glass 家族

### 1.5 业务入口 chrome
- 文件管理导航条
- Agent toolbar
- 知识库 workspace head
- Office / Content Studio 顶栏

### 1.6 能力（V1–V3 继承）
MenuBar / Dock magnify+bounce / Launchpad / Spotlight / App Switcher / 交通灯 / genie 近似 / 图标分层 / 去 Element 桌面反馈

### 1.7 验证
```bash
cd frontend && npx playwright test \
  tests/desktop-macos-shell.spec.mjs \
  tests/desktop-notification-center.spec.mjs
# 16 passed
```

---

## 2. 明确不做 / 留给 Win 阶段

1. Win11 完整 Taskbar / Start / 窗口右上控件投影
2. 摄影级商业壁纸库（当前 SVG 已够喂 blur；可再换 jpg）
3. 业务深页逐屏扫（列表/编辑器内页）——入口 chrome 已统一
4. 深色模式全集（token 预留，未做切换 UI）

---

## 3. Win11 下一阶段入口（未开工）

1. 在 `skins/win11.ts` 上补组件条件渲染（taskbar/start）
2. 窗口控件皮肤分支
3. 设置页皮肤开关 UI
4. 不改 Product/Content 主路径

---

## 4. 关键路径

- `frontend/src/desktop/skins/*`
- `frontend/src/desktop/design-system/desktop-design-tokens.css`
- `frontend/src/styles/desktop-shell.css`
- `frontend/public/desktop/wallpaper-macos-default.svg`
- `frontend/src/desktop/menubar/desktop-control-center.vue`
- `frontend/src/desktop/feedback/desktop-toast-host.vue`
- `frontend/src/platform/components/apps/desktop/file-manager/fm-navigation-bar.vue`
- `modules/agent/frontend/index.vue`
- `modules/knowledge/frontend/index.vue`
- `products/office/frontend/index.vue`
- `products/content-studio/frontend/index.vue`
