# 底座未完成收口清单

> 日期：2026-07-18
> 状态：**未落地**
> 来源：蒸馏临时文档 02/03/05/06；只保留未完成项与验收口径。

## 0. 已落地（勿重复施工）

| 项 | 状态 | 落点摘要 |
|----|------|----------|
| 三协议流式归一 | 基本完成 | gateway adapters 统一 `StreamEvent`；含 responses / anthropic |
| 召回重排默认开 | 完成 | `use_rerank` 默认 True；阈值 0.3 已接 |
| Product + Content Runtime 主路径 | 完成 | products / content open / draft-save-lease |
| 桌面 macOS 信息架构 V1 | 完成 | MenuBar/Dock/LP/Spotlight（视觉见另一临时文档） |

## 1. 未完成（按伤害排序）

### 1. 模型网关收口（高）— **TTL 自愈已落地；残留直连仍待**

问题：知识库侧 `model_routing` 熔断/暂停与网关 retry-fallback 并存，429 后可能永久 pause、无 TTL 自愈。

已做：

1. 网关仍负责 retry/fallback（知识库不抢降级决策）
2. `pause_model_stage_queue` 写入 `resume_ttl_seconds` + `expires_at`（默认 900s，可配）
3. `maybe_clear_expired_model_auto_pause` + dispatcher 加载配置时自动清理过期 pause
4. 存量永久 pause 会 backfill TTL；过期后自动恢复队列
5. `models.json` `module_routing.knowledge.rate_limit_auto_pause.resume_ttl_seconds`

仍待：

1. 收编残留直连（低优先：生图等）

验收：制造一次 429，管线只临时 pause；TTL 后自动继续。

相关：`modules/knowledge/**/model_routing*`、`backend/app/services/task_dispatcher.py`、`models.json`

### 2. PgBouncer 真接通（中）

问题：进程可能在跑，但后端仍直连 5432、大池配置，等于空转。

要做：

1. `DATABASE_URL` / `DB_PORT` 指 PgBouncer
2. 应用侧池改小（真池在 bouncer）
3. asyncpg + transaction 模式：`statement_cache_size=0`
4. executor 子进程同样走 bouncer

验收：6432 有连接、5432 归零、并发不再 too many clients。
回退：改回直连端口。

### 3. 召回阈值调优（中低）

问题：`RERANK_SCORE_THRESHOLD=0.3` 为经验值，未用真实 query 标注反推。

要做：20–30 条真实 query 标注 → 调阈值；reranker 冷启动常驻可选。

验收：噪音明显下降且不伤主要命中。

### 4. 三协议小尾巴（低）

问题：ant 工具流式 `input_json_delta` 参数累积是否被上层完整消费，需真调确认。

验收：deepseek-anthropic / claude 路径工具调用流式参数完整。

### 5. 资源监视前端（低，可并桌面视觉）

后端资源/进程接口已有；桌面“活动监视器”应用未建。
可并入桌面视觉/产品壳任务，不阻塞底座。

### 6. 系统资源缓存 / API 统计（低）

若尚未统一：

- 资源采样缓存供 dispatcher/status 毫秒读
- 请求日志带来源标识与聚合，便于抓死轮询

以代码现状为准，做前先 `rg` 确认是否已有实现。

## 2. 明确不做 / 另册

- 桌面 Liquid Glass 视觉 → `01_桌面壳LiquidGlass视觉收口_20260718.md`
- 规则优先实体抽取 → `03_规则优先实体抽取_待落地_20260718.md`
- 任何数据库回填/约束 finalize/读者翻转操库 → 不在本清单（华哥另派）

## 3. 推荐顺序

1. 模型网关收口
2. PgBouncer 接通（低峰）
3. 召回阈值调优
4. 三协议小尾巴
5. 资源监视前端（可并行给前端）
