# Agent 升级执行总信（2026-06-25）

这份文件不是研究笔记，而是给执行 agent 的母信底稿。  
研究证据、交叉印证、补充推演，都保留在：

- `开发文档/Agent升级方向总图-2026-06-25.md`

如果只发一封总信，就发这一份。  
如果后续拆分成多封子信，也必须从这一份拆，不要再从研究总图重新摘抄。

## 这封信到底解决什么

这封信不是为了证明“哪些点值不值得做”。  
这一步已经结束了。

这封信现在只解决 4 件事：

1. 当前 V2 每个升级节点，真实逻辑在哪个文件、哪几段函数
2. 参考后更强的逻辑是什么，不是只写项目名
3. 这一段里谁最强，为什么是它强
4. 如果要把命中率和稳定性继续往上推，我们手里有哪些精度控制手柄可以自己拧

你可以把它理解为：

- `Agent升级执行总信-2026-06-25.md` = 后续所有开发 agent 的施工母信
- `Agent升级方向总图-2026-06-25.md` = 这封母信后面的证据仓和交叉印证仓

## 用法

执行 agent 接手时，固定按下面顺序阅读：

1. 本文件
2. `开发文档/Agent升级方向总图-2026-06-25.md` 中对应节点
3. 本文件里给出的 V2 源码文件 + 行号
4. 本文件里给出的参考源码文件 + 行号

## 回信格式（强制）

每个执行 agent 回信时，固定按这个格式：

1. 改了哪些文件
2. 每个文件改了什么
3. 跑了哪些验证
4. 当前节点成熟度从什么等级升到什么等级
5. 剩余风险是什么

不要只回“已完成”“已修复”“已优化”。

## 成熟度分级

- `M1 Experimental`
  - 代码已落，但只过最小验证
- `M2 Alpha`
  - 主链路可跑，已过针对性验收
- `M3 Beta`
  - 角色 / 治理 / 失败路径已经补齐
- `M4 Stable`
  - 可作为后续节点稳定依赖

## 节点总表（一眼看清）

| 节点 | 当前 V2 主入口 | 当前短板 / 剩余工作 | 关键参考 | 下一步需深化 |
|---|---|---|---|---|---|
| A1 运行时主循环 ✅ | `chat.py:24` + `runtime/`(ConversationRuntime/ToolLoopRuntime/StreamEmitter/TaskSink/RuntimePolicy) | 已落地；policy 粒度待细化、durability 待整合 | Claude + LangGraph | policy 与 understanding 联动 |
| A2 技能系统 ✅ | `skills_loader.py:26-301` + `skill_governance_service.py:35-463` | 已落地；长期质量反馈闭环未打通 | Hermes + Claude | 命中率 → 自动降权闭环 |
| A3 记忆精修 ✅ | `layered_memory.py:444-893`(selector+fencing+snapshot) + `engine.py:135-177` | 已落地；archival 未独立层 | Letta + Hermes | archival 层独立 |
| A4 知识库证据面 ✅ | `search_service` + `fusion_service` + `KbEvidence` + evidence capabilities | 已落地；multi-hop 未深入 runtime | LangGraph + Dify | multi-hop evidence planning |
| A5 模型治理面 ✅ | `router.py:79-126`(ModelRouter) + `:279-628`(ModelGatewayRouter) + `config.py`(Templates/Policies) | 已落地；前端管理台未做 | OpenCode + OpenClaw | 模型管理台 UI |
| A6 理解环 ✅ | `runtime/understanding_loop.py`(4 角色 + packet) + `conversation_runtime.py:106-140` | 已落地；子 agent 角色配置不灵活 | Claude + Hermes | 角色注册 + 并发理解 |
| A7 能力/事件治理 ✅ | `module_registry.py:52-205`(register + call + list) + `module_events.py:39-83` | 已落地；version 演进策略 + 死信待做 | OpenClaw + Coze | version_range + dead_letter |
| A8 文件→资产系统 🔄 | `asset_service.py:25-192` 已落, `runtime/task_sink.py` 待挂 artifact | 资产模型已落，tool→artifact 自动落未完成 | Letta + OpenHands | `task_sink.record_assets` 挂接 |
| A9 框架稳固性 ✅ | `task_worker.py:55-274` + `gateway/router.py:26-126` + `post_turn_hooks.py:141-707` | 已落地；checkpoint/replay 未整合 | LangGraph + OpenClaw | checkpoint 整合 |
| A10 工具台工作流 📋 | `dev_toolkit/server.py:613-650, 675-1123` + `engine.py:299-308`(dev_workflow 注入) | 工具强但未强制驱动 agent 行为 | Claude + Hermes | probe→memory 联动 |
| A11 文档 IR 中间层 | `office.py:7-80` + `docs-open/content.py:16-132` + parser 链 | 各格式 JSON schema 分叉，未统一 IR | Docling + Unstructured | 统一 DocumentIR |
| A12 权限协作控制面 | `file_share_service.py:9-192` + `file_service.py:164-177` + `role_service.py:9-58` | 只有 file 级 owner/share/read/edit | OpenClaw + Claude | 协作控制面最小版 |

当前节点状态：✅ 已落地（06） 🔄 进行中（07） 📋 待执行

## 先看 V2 现在到底已经有什么

不要把这轮升级理解成"从白板再造一个 agent"。

V2 经过轮次 01-06 升级后，已有 4 层完整骨架：

1. 会话执行层（A1+A6 ✅）
   - `modules/agent/backend/runtime/` — ConversationRuntime / ToolLoopRuntime / StreamEmitter / TaskSink / RuntimePolicy / UnderstandingLoopOrchestrator
   - `modules/agent/backend/handlers/chat.py:24` — 仅做 HTTP/SSE 适配
   - `modules/agent/backend/engine/engine.py:32-206` — 上下文装配
   - 已有：SSE、工具循环、stuck detector、diminishing returns、post-turn hooks、理解环、artifact 落库
2. 认知材料层（A2+A3+A4 ✅）
   - `modules/agent/backend/engine/skills_loader.py:26-301` + `skill_governance_service.py:35-463` — 技能治理
   - `modules/agent/backend/engine/layered_memory.py:444-893` — 三层记忆 + selector + fencing + snapshot
   - `modules/knowledge/backend/services/` — 检索 + evidence packet + answerability
   - 已有：skills 治理、三层记忆、知识库证据包、图谱上下文
3. 平台控制层（A5+A7+A9 ✅）
   - `backend/app/gateway/router.py:79-628` — ModelRouter + ModelGatewayRouter 模型治理
   - `backend/app/services/module_registry.py:52-205` — 能力治理控制面
   - `backend/app/services/module_events.py:39-83` — 事件治理
   - `backend/app/services/task_worker.py:55-274` — 框架稳固底座
   - 已有：模型路由、能力注册与治理、事件总线、任务 worker、统一诊断
4. agent-first 工程层
   - `dev_toolkit/server.py:613-650, 823-1123` — codegraph / probe / call_capability / memory_write
   - `modules/agent/backend/engine/engine.py:299-308` — dev_workflow 注入段
   - 已有：brief、codegraph、probe、call_capability、memory_write

所以剩余升级（A8/A10/A11/A12）是把已有骨架的平台化程度继续做深。

## 总执行顺序（01-06 已完成，当前为 W1 批次）

轮次 01-06 已完成 A1-A7 和 A9 的落地验收。当前剩余节点按依赖关系推进：

1. ✅ A1 运行时主循环（轮次 02）
2. ✅ A2 技能系统（轮次 03）
3. ✅ A3 记忆精修（轮次 04）
4. ✅ A4 知识库证据控制面（轮次 04）
5. ✅ A5 模型治理控制面（轮次 05）
6. ✅ A6 理解增强型多代理（轮次 05）
7. ✅ A7 平台控制面 / 能力治理（轮次 06）
8. ✅ A9 框架稳固性补强（轮次 06）
9. 🔄 A8 文件系统升级为资产系统（轮次 07，进行中）
10. 📋 A10 项目工具台默认工作流
11. 📋 A11 统一文档 IR 中间层
12. 📋 A12 多人权限与分享协作控制面

W1 批次（可并行）：
- A8 收尾（asset_service 已落、task_sink 挂接 artifact）
- A10 第一刀（probe→memory_write 联动 + dev_workflow 注入增强）
- A12 第一刀（分享模型升 expiry/scope/audit）

## A1 运行时主循环重构 ✅（轮次 02 已落地）

### 当前 V2 逻辑（落地后状态）

- `modules/agent/backend/handlers/chat.py:24-27`
  - `handle_chat()`: 仅做 HTTP/SSE 适配 — 创建 `ConversationRuntime` 后调 `runtime.execute()`
- `modules/agent/backend/runtime/conversation_runtime.py:34-197`
  - `ConversationRuntime`: 编排全对话回合 — init、用户消息持久化、`assemble_context`、理解环、工具发现、子 runtime 接线
- `modules/agent/backend/runtime/tool_loop_runtime.py:39-564`
  - `ToolLoopRuntime.run()`: 工具循环 — stuck detector、diminishing returns、budget pressure、inline tool recovery
- `modules/agent/backend/runtime/stream_emitter.py`
  - `StreamEmitter`: 可复用的 SSE 流式发射器
- `modules/agent/backend/runtime/task_sink.py:31-212`
  - `RuntimeTaskSink`: assistant 消息持久化、事件持久化、timeline、post-turn hooks、artifact 落库
- `modules/agent/backend/runtime/runtime_policy.py`
  - `RuntimePolicy`: 统一运行时策略对象 — 收口 MAX_TOOL_ROUNDS/stuck/diminishing/budget/stream recovery
- `modules/agent/backend/runtime/understanding_loop.py`
  - `UnderstandingLoopOrchestrator`: 理解环（高歧义任务触发）
- `modules/agent/backend/engine/engine.py:32-206`
  - `assemble_context()`: prompt / budget / skills / memory / experience 注入（保留上下文职责，不承接运行时编排）
- `modules/agent/backend/engine/engine.py:252-312`
  - `_build_system_content()`: 分段装配系统提示词（含 dev_workflow 注入段）
- `modules/agent/backend/engine/engine.py:322-346`
  - `get_orchestrator()` + `get_hooks()` + `get_budget_tracker()`: 模块级单例（runtime service factory 形态）

当前主链路是：

1. `handle_chat()` → 创建 `ConversationRuntime` → 调 `runtime.execute()`
2. `ConversationRuntime.execute()` 持久化用户消息 → `assemble_context()` 注入
3. 可选理解环（`UnderstandingLoopOrchestrator`）→ 产 `understanding_packet`
4. `ToolLoopRuntime.run()` 跑工具循环（stuck / diminishing / budget policy 由 `RuntimePolicy` 集中管理）
5. `RuntimeTaskSink` 处理 assistant 持久化、timeline、post-turn hooks、artifact 落库
6. `StreamingResponse` 经 `_event_stream()` 生成器返回

当前剩余短板：

- policy 已收口但粒度还不够细（无任务复杂度自适应）
- understanding loop 已接入但与 runtime policy 的联动还不深
- durability / checkpoint 未整合进 runtime 核心

### 参考后逻辑（保留作为后续 A9 深化参考）

- Claude：
  - `src/tools/AgentTool/built-in/planAgent.ts:14-91`
  - `src/tasks/LocalMainSessionTask.ts:1-110`
- LangGraph：
  - `libs/langgraph/langgraph/pregel/main.py`
  - `libs/langgraph/langgraph/pregel/_loop.py`
- OpenHands：
  - conversation runtime / workspace 生命周期闭环

### 提精度手柄（已收口到 `RuntimePolicy`）

- `MAX_TOOL_ROUNDS` → `RuntimePolicy.max_tool_rounds`
- stuck detector → `RuntimePolicy.enable_stuck_detection`
- diminishing returns → `RuntimePolicy.enable_diminishing_returns`
- budget pressure → `RuntimePolicy.budget_pressure_threshold`
- memory recall quality → `RuntimePolicy.min_memory_quality_for_injection`
- stream 后 inline tool recovery 开关 → `RuntimePolicy.enable_inline_tool_recovery`

### 验收（已通过，轮次 02 真数据过）

- `/api/agent/chat` SSE 行为不退化
- stuck / diminishing 仍生效
- post-turn hooks 触发时机不变
- `agent_events` 与 `agent_message_meta.timeline` 语义不变

## A2 技能系统工业化 ✅（轮次 03 已落地）

### 当前 V2 逻辑（落地后状态）

- `modules/agent/backend/engine/skills_loader.py:26-301`
  - `SkillDef(:26)` / `resolve_skill_priority(:53)` / `SkillsLoader(:109)`
  - `find_skills(:122)` / `match_skills(:167)` / `format_skills_for_prompt(:198)`
- `modules/agent/backend/engine/engine.py:96-122`
  - skills 注入 — 读治理注册表后经 `find/match/format` 注入到 system prompt
- `modules/agent/backend/services/skill_governance_service.py:35-463`
  - `list_skills(:35)` / `get_skill(:67)` / `create_skill(:94)`
  - `update_skill(:139)` / `delete_skill(:191)`
  - `list_pending_skill_approvals(:216)` / `resolve_skill_approval(:241)`
  - `request_skill_approval(:287)` / `record_skill_usage(:315)` / `get_skill_usage_stats(:339)`
  - `scan_file_skills_to_registry(:388)` / `get_skill_provenance(:463)`

当前已有：

- markdown 扫描 + frontmatter 解析 + scope/priority + path 匹配
- skill CRUD + approval + scan + provenance + usage
- 治理注册表 + 审批门 + 使用统计
- `scan_file_skills_to_registry` 把磁盘 skill 文件同步到注册表
- review fork 可产出 skill proposal（经审批门后落地）

当前剩余短板：

- 没有"哪些 skill 在什么任务上有用"的长期质量反馈闭环
- skill 命中率与经验库未打通
- 按任务类型自动过滤 / 低频 skill 自动降权仍只有前端信号，无后端自动治理

### 参考后逻辑（保留作为后续深化参考）

- Hermes：skill lifecycle / background review → skill proposal
- Claude：`src/skills/loadSkillsDir.ts:78-185`, `453-458`

### 提精度手柄（已落地）

- skill 注入数量上限（engine.py 中 signal-aware 裁剪）
- 未审批 skill 禁止自动注入（审批门）
- 按 workspace/path 命中率排序（usage stats）
- 低质量 / 高失败率 skill 降权（usage 信号，待自动治理）

### 验收（已通过，轮次 03 真数据过）

- 至少支持 skill CRUD + approval + usage
- review fork 只能产 proposal，不能直接写正式 skill
- skill 注入继续兼容 `<available_skills>`

## A3 记忆精修 ✅（轮次 04 已落地）

### 当前 V2 逻辑（落地后状态）

- `modules/agent/backend/engine/layered_memory.py:444-893`
  - `MemorySelectionResult(:444)` / `select_memory_segments(:458)`: 选择器
  - `apply_fencing(:546)`: 围栏
  - `get_frozen_snapshot(:631)`: 冻结快照
  - `recall_stable_rules(:644)` / `recall_chunk(:688)` / `recall(:343)`: 三层召回
  - `three_layer_recall(:779)`: 合并三层 + selector + fencing + quality_score
  - 注入格式函数: `_format_rules_injection(:896)` / `_format_chunks_injection(:905)` / `_format_semantic_injection(:920)`
- `modules/agent/backend/engine/engine.py:135-177`
  - `three_layer_recall()` 注入 — 含 selector 输出 + quality_score 信号发射
- `modules/agent/backend/models.py`
  - `AgentRecallQuality(:168)` / `MemorySnapshot(:238)` / `ContextSnapshot(:137)`

当前已有：

- stable rules + chunk recall + semantic recall + experience 注入
- recall quality signal（`AgentRecallQuality`）
- **selector**（`select_memory_segments`）— recall 后非盲注入
- **fencing**（`apply_fencing`）— 分层裁剪
- **frozen snapshot**（`get_frozen_snapshot` / `MemorySnapshot`）— 长任务快照复用
- working memory / frozen snapshot 已分出边界

当前剩余短板：

- archival memory 还没有独立层（当前三层都在一套 provider 内）
- 选择器的特征维度还不够细（暂无任务类型感知裁剪）
- 冻结快照的自动过期管理还不完善

### 参考后逻辑（保留作为后续深化参考）

- Letta：core / recall / archival 严格分层
- Hermes：memory fencing / frozen snapshot / 二次蒸馏
- Claude：relevant memory selection

### 提精度手柄（已落地）

- `max_memory_segments` → `select_memory_segments()` 参数
- `min_relevance_score` → `MemorySelectionResult` 裁剪阈值
- `freeze_snapshot_for_long_task` → `three_layer_recall(frozen_key=...)`
- `stable_rules_hard_cap` / `chunk_vs_semantic_ratio` 已内建

### 验收（已通过，轮次 04 真数据过）

- 注入字符数下降但相关度不下降
- 长任务 snapshot 可复用
- recall quality 指标仍可观测

## A4 知识库升级为证据控制面 ✅（轮次 04 已落地）

### 当前 V2 逻辑（落地后状态）

- `modules/knowledge/backend/services/search_service.py`
  - `keyword_search()` / `vector_search()` / `rrf_fusion()` / `hybrid_search()` 保留
  - 升级：返回带 `evidence_meta`/`citation`/`_trace` 的结果
- `modules/knowledge/backend/services/fusion_service.py`
  - 融合 chunk 已能回吐 citation / provenance / page 级证据
- `modules/knowledge/backend/services/entity_service.py`
  - 图谱上下文已并入 evidence packet 输出
- `modules/knowledge/backend/models.py`
  - `KbEvidence(:188)` / `KbConclusionEvidence(:202)` — 证据包持久化
- `modules/knowledge/backend/router.py`
  - 新增 `evidence_search` / `get_evidence_detail` 等 evidence-oriented capability

当前已有：

- keyword / vector / RRF / rerank + page fusion + entity graph
- **evidence packet** — Agent 侧收到的不再是纯文本块，而是 `{evidence_meta, citation, source, confidence}` 结构
- **answerability** — 检索结果可区分"可判定 / 证据弱 / 不可判定"
- **query rewrite** — 检索前规则化 query 改写
- `knowledge:plan_evidence` / `knowledge:judge_answerability` / `knowledge:get_citation_package` 等能力已注册

当前剩余短板：

- multi-hop evidence planning 还未深度集成到 agent runtime 侧
- Agent 侧调用仍偏简单（调一次 search 就结束，而非按 plan 多轮收敛）
- 知识库可观测性（Dify 式的 pipeline 视图）尚未接入

### 参考后逻辑（保留作为后续深化参考）

- LangGraph：retrieval grading / query rewrite / self-RAG / adaptive RAG
- Dify：dataset / pipeline / observability
- Coze：knowledge → workflow/resource graph 节点

### 提精度手柄（已落地）

- 是否允许 multi-hop → `evidence_search` 参数
- 是否必须带 citation package → `knowledge:plan_evidence` 能力参数
- 证据不足是否禁止直接下结论 → `answerability` 判定
- query rewrite 最大轮数
- graph context 是否参与最终证据包

### 验收（已通过，轮次 04 真数据过）

- Agent 能收到 evidence packet，不只是文本块
- 检索失败 / 证据弱 / 不可判定可区分
- knowledge 能同时服务 chat、review、workflow

## A5 模型治理控制面 ✅（轮次 05 已落地）

### 当前 V2 逻辑（落地后状态）

- `backend/data/config/models.json`
  - 四层配置：provider / model capability / variant / policy
- `backend/app/gateway/config.py:11-16`
  - `VariantTemplate` dataclass、`TEMPLATES`/`ROUTING_POLICIES`/`BUDGET_RATES`
  - `resolve_template_for_role()` / `list_templates()` / `list_routing_policies()`
- `backend/app/gateway/router.py:79-126`
  - `ModelRouter` — 治理层模型路由器：resolve_template / resolve_profile_key / get_diagnostics
- `backend/app/gateway/router.py:279-628`
  - `ModelGatewayRouter` — 执行层：chat / chat_stream / describe_image / generate_image / check_all_health
  - 含 `RetryBudget(:26)` + `_call_with_retry(:237)`
- `backend/app/gateway/service.py:42-88`
  - `get_model_provider()` / `get_fallback_chain()` — 稳定公共接口
- `modules/agent/backend/models.py:37-64`
  - `AgentConfig` 已支持 provider / model / fallback_model / daily_budget / monthly_budget
- `modules/agent/backend/engine/fallback_chain.py`
  - 降级链：主模型 → fallback → 本地兜底
- `backend/app/gateway/router.py:229-234`
  - `list_model_profiles()` — 公共模型列表接口

当前已有：

- 四层配置对象：provider → model_capability → variant_template → routing_policy
- `ModelRouter` 控制面（template + routing + budget/health/diagnostics）
- `ModelGatewayRouter` 执行面（chat/stream/retry/fallback）
- planner/reviewer/executor 模板可分开配置（`resolve_template_for_role`）
- health / budget / rate / failover 诊断可输出
- 429/timeout 自动切 provider（`_call_with_retry` + `RetryBudget`）

当前剩余短板：

- 模型模板与 agent config 的联动还不够深（`AgentConfig` 尚未引用 role template）
- 前端模型管理台仍只有 admin 端点，无 UI

### 参考后逻辑（保留作为后续深化参考）

- OpenCode：provider / variant / budget / TPM limiter
- OpenClaw：failover / model config 作为控制面对象
- Hermes：多 provider 天然切换

### 提精度手柄（已落地）

- 任务阶段切模 → `resolve_template_for_role(role, ...)`
- 预算紧张降模 → `budget_tight=True` 路由参数
- 高歧义任务升模 → `high_ambiguity=True` 路由参数
- 429/timeout 自动切 provider → `_call_with_retry` + `RetryBudget`
- streaming / non-streaming 分路由 → `chat()` / `chat_stream()` 独立路径

### 验收（已通过，轮次 05 真数据过）

- planner/reviewer/executor 模板可分开配置
- fallback 不再只是线性链
- health / budget / rate / failover 可诊断

## A6 理解增强型多代理循环 ✅（轮次 05 已落地）

### 当前 V2 逻辑（落地后状态）

- `modules/agent/backend/runtime/understanding_loop.py`
  - `UnderstandingLoopOrchestrator`: 理解环编排 — `should_trigger()` / `run()`
  - 4 只读理解角色：`intent_clarifier` / `concern_miner` / `plan_critic` / `retrieval_evidence`
  - 输出统一 `understanding_packet`（含 intent / concerns / plan_critique / retrieval_evidence / confidence）
- `modules/agent/backend/runtime/conversation_runtime.py:106-140`
  - 理解环触发点 — 高歧义/高代价任务默认触发，产 `understanding_diag` 事件
- `modules/agent/backend/handlers/tool.py:119`
  - `_cap_spawn_subagent()` — 子 agent 入口（角色可配，理解角色和执行角色共享同一通道）
- `modules/agent/backend/engine/engine.py`
  - `assemble_context()` 已整体注入，理解环注入在 runtime 侧完成

当前已有：

- 4 只读理解角色 + 统一 `understanding_packet`
- 仅高歧义/高代价任务触发（`RuntimePolicy.enable_understanding_loop` + `understanding_min_chars`）
- 最多 2-3 轮理解 → 产出结构化 packet → 注入 system prompt
- understanding_diag 事件持久化（可审计）
- 理解环预算上限

当前剩余短板：

- 理解环与 `plan_critic` 输出的联动还不是双向闭环
- 子 agent 角色配置面还不够灵活（暂无运行时动态角色注册）
- 多头像并发理解（如 plan + retrieval 并行）未做

### 参考后逻辑（保留作为后续深化参考）

- Claude：只读 plan agent 角色
- Hermes：delegation / review fork
- OpenClaw：multi-agent routing 控制面

### 提精度手柄（已落地）

- 仅高歧义 / 高代价任务触发 → `RuntimePolicy.enable_understanding_loop` + `understanding_min_chars`
- 最多 2-3 轮 → `UnderstandingLoopOrchestrator` 内部轮次上限
- 证据不足不进入执行态 → `understanding_packet.confidence` 阈值
- 理解环预算上限 → 独立于主 budget 的 understanding budget
- 是否强制引用 evidence packet → A4 evidence packet 注入

### 验收（已通过，轮次 05 真数据过）

- 简单任务不明显变慢
- 高歧义任务理解更稳
- understanding packet 可持久化与审计

## A7 平台控制面 / 能力治理 / 事件治理 ✅（轮次 06 已落地）

### 当前 V2 逻辑（落地后状态）

- `backend/app/services/module_registry.py:52-91`
  - `register_capability()`（:52）— 参数含 contract_version / timeout / trusted_callers / side_effect_level / retry_policy / brief
- `backend/app/services/module_registry.py:96-118`
  - `_resolve_caller_role()`（:96）
- `backend/app/services/module_registry.py:120-175`
  - `call_capability()`（:120）— 带 trace_id / retry_attempt / caller_role 解析
- `backend/app/services/module_registry.py:176-205`
  - `list_capabilities()`（:176）— 按角色过滤 + 返回完整治理 metadata
- `backend/app/services/module_events.py:39-83`
  - `register_module_event_handler()`（:39）/ `emit_module_event()`（:83）
- `backend/app/routers/modules.py:18-32`
  - `/api/modules/capabilities` 返回含 owner/risk/timeout/contract 信息

当前已有：

- capability metadata（contract_version / timeout / trusted_callers / side_effect / retry_policy）
- event trace（trace_id / retry / delivery metadata）
- /capabilities 返回治理信息

当前剩余短板：

- capability version 兼容（contract_version 有但无版本演进策略）
- capability 热加载 / 健康检查
- 事件死信 / 重放

### 先改哪几段函数

1. `backend/app/services/module_registry.py:52`
   - `register_capability()` 补 version_range 兼容性声明
2. `backend/app/services/module_registry.py:176`
   - `list_capabilities()` 支持 version 过滤
3. `backend/app/services/module_events.py:45`
   - `emit_module_event()` 补 dead_letter / replay 字段

### 验收

- capability list 带治理字段（已有）
- event 带 trace / delivery / retry 字段（已有）
- agent 调用前能读取治理信息（已有）
- 剩余：version 兼容示例可验证

## A8 文件系统升级为 agent 资产系统 🔄（轮次 07 进行中）

### 当前 V2 逻辑（部分落地状态）

- `backend/app/services/asset_service.py:25-192`
  - `create_asset()`（:25）— 创建资产记录，支持 draft / published / evidence / generated / handoff
  - `update_asset_state()`（:68）— 更新资产语义类型或发布状态
  - `get_asset()`（:96）/ `get_assets_by_conversation()`（:101）/ `list_user_assets()`（:121）
  - `publish_asset()`（:155）/ `delete_asset()`（:164）
- `backend/app/models/asset.py`
  - `FileAsset` 模型：file_id / owner_id / asset_type / publish_state / conversation_id / tool_name / tool_call_id / source_file_id / provenance
- `backend/app/routers/assets.py`
  - `POST /api/assets` / `GET /api/assets/{id}` / `GET /api/assets` / `PATCH /api/assets/{id}/publish` / `DELETE /api/assets`
- `backend/app/services/file_service.py:125-144`
  - `delete_to_trash()`（:125）
- `backend/app/services/file_service.py:164-177`
  - `check_file_access()`（:164）
- `modules/agent/backend/runtime/task_sink.py:153-212`
  - `record_assets()`（:153）— tool 输出自动落 artifact（已实现方法，待 runtime 主循环调用）

当前已有：

- `FileAsset` 模型含 5 种 asset_type + publish_state
- asset CRUD 端点
- provenance、conversation/tool/source_file 关联
- 文件访问边界仍然通过 `check_file_access()`
- `task_sink.record_assets()` 已实现（可跑），但未被 runtime 主循环自动调用

当前缺失（需继续）：

- tool 输出自动落 artifact 的 runtime 挂接（`task_sink.record_assets` 已写好，只需在 `ToolLoopRuntime.run()` 结束后调一次）
- 删除语义区分 draft/published/evidence
- workspace working set 自动维护
- 上传/生成/发布/引用事件链

### 先改哪几段函数

1. `modules/agent/backend/runtime/tool_loop_runtime.py:65`
   - `ToolLoopRuntime.run()` tool 循环结束后调 `sink.record_assets()` 自动落 artifact
2. `backend/app/routers/file_transfer.py`
   - upload 路由区分普通上传 vs asset 上传
3. `backend/app/services/file_service.py:125`
   - `delete_to_trash()` 补 asset_type 感知：draft 可静默删除，published 需确认

### 验收

- 产物可回溯 conversation / tool / source file
- draft / published / evidence 可区分
- knowledge / docs-open / office-gen 可复用

## A9 框架稳固性补强 ✅（轮次 06 已落地）

### 当前 V2 逻辑（落地后状态）

- `backend/app/services/task_worker.py:55-94`
  - `_reconcile_one_orphan()`（:55）
  - `_recover_stale_tasks()`（:67）
  - `_recover_orphan_running_tasks()`（:94）
- `backend/app/services/task_worker.py:125-258`
  - `_claim_one_task()`（:125）
  - `_run_handler()`（:157）
  - `_finish_task()`（:201）— 带 trace / retry_reason / state_version
  - `_worker_loop()`（:235）
- `backend/app/services/task_worker.py:258-274`
  - `start_worker()`（:258）/ `stop_worker()`（:264）/ `worker_health()`（:274）
- `backend/app/gateway/router.py:26-78`
  - `RetryBudget`（:26）— retry tracking
- `backend/app/gateway/router.py:237-277`
  - `_call_with_retry()` — health / failover 诊断
- `backend/app/gateway/router.py:618-628`
  - `check_all_health()`
- `backend/app/services/module_registry.py:52-205`
  - capability 治理字段（contract_version/timeout/trusted_callers/side_effect/retry_policy）
- `backend/app/services/module_events.py:39-83`
  - 事件治理字段（delivery_mode/retry_policy/idempotency_key/trace_id）
- `modules/agent/backend/engine/post_turn_hooks.py:141-176, 504-707`
  - 统一诊断语义（lease_owner / heartbeat_at / retry_reason / state_version）

当前已有：

- 统一 worker/task/hook/model/capability 诊断
- 多 worker FOR UPDATE SKIP LOCKED 无退化
- stale/orphan recovery、lease claim、heartbeat
- capability/event 共用治理字段

当前剩余短板：

- replay / checkpoint 未整合进框架核心
- event 死信 / 重放策略未产品化

### 先改哪几段函数

1. `backend/app/services/task_worker.py:201`
   - `_finish_task()` 补 `checkpoint_ref` 字段
2. `backend/app/services/module_events.py:45`
   - `emit_module_event()` 补 dead_letter_queue 机制
3. `backend/app/services/task_worker.py:274`
   - `worker_health()` 输出支持 checkpoint granularity

### 验收

- worker/task/hook/model/capability 都能输出统一诊断（已有）
- 多 worker 语义不退化（已有）
- 后续其他升级节点可复用这套底座（已有）
- 剩余：checkpoint 字段可观测

## A10 项目工具台 / CodeGraph / Codemap 吸入默认工作流 📋（待执行）

### 当前 V2 逻辑

- `dev_toolkit/server.py:613-650`
  - `_code_explore()`（:613）/ `_code_node()`（:624）/ `_code_impact()`（:635）
- `dev_toolkit/server.py:675-791`
  - `_routes()`（:675）/ `_capabilities()`（:714）/ `_db_schema()`（:756）/ `_run_test()`（:795）
- `dev_toolkit/server.py:823-941`
  - `_brief()`（:823）/ `_assess_evidence()`（:943）/ `_probe()`（:971）/ `_call_capability()`（:999）
- `dev_toolkit/server.py:1025-1123`
  - `_MATURITY_SCORE`（:1025）/ `_maturity()`（:1085）
- `dev_toolkit/server.py:1124-1375`
  - `_tail_log()`（:1124）/ `_log_errors()`（:1173）/ `_sql()`（:1195）/ `_web_read()`（:1205）
  - `_memory_search()`（:1248）/ `_memory_write()`（:1326）/ `_memory_recent()`（:1373）
- `modules/agent/backend/engine/engine.py:299-308`
  - `_build_system_content()` 已含 `<dev_workflow>` 注入段

当前已有：

- 全套工具台 MCP 工具
- engine.py 已有默认 `dev_workflow` 注入段
- memory_write 已有项目记忆闭环

当前剩余短板：

- 工具台是"存在"但未强制驱动 agent 行为
- probe 结果未自动触发 memory_write
- codegraph/codemap 偏差未回写反馈
- 命中率未沉淀为技能

### 先改哪几段函数

1. `dev_toolkit/server.py:971`
   - `_probe()` 结果自动调 `_memory_write()` 记录验证结论
2. `dev_toolkit/server.py:613-635`
   - `_code_explore()` / `_code_impact()` 结果补偏差反馈入口
3. `modules/agent/backend/engine/engine.py:299-308`
   - dev_workflow 注入段补详细阶段说明（brief→codegraph→probe→memory_write）
4. `modules/agent/backend/services/skill_governance_service.py`
   - 命中率反馈自动产 skill proposal

### 验收

- agent 默认更少猜代码
- 调研 / 修复 / 验收三段式更稳定
- project memory 可累积

## A11 统一文档 IR 中间层

### 当前 V2 逻辑

- `backend/app/models/office.py:7-80`
  - `FileJsonPackage` / `FileJsonVersion` / `FileJsonPatch` / `FileJsonTask`
- `backend/app/services/office/json_package_service.py:18-94`
  - `get_status()` / `create_package()` / `read_package()`
- `backend/app/routers/office.py:29-175`
  - status / package / versions / patches / preview / apply / rollback
- `backend/app/routers/office_export.py:38-113`
  - 现有 package -> docx/xlsx/pptx 导出
- `modules/docs-open/backend/router.py:205-240`
  - `GET/POST /api/docs/{file_id}/content`
- `modules/docs-open/backend/handlers/content.py:16-87`
  - `_read_content()`
- `modules/docs-open/backend/handlers/content.py:90-132`
  - `_write_content()`
- `modules/text-parser/backend/router.py:35-137`
- `modules/docx-parser/backend/router.py:35-101`
- `modules/xlsx-parser/backend/router.py:35-108`
  - 这三类 parser 已经在回 `{file_id, format, blocks, resources}` 风格结构

当前不是“完全没有统一文档层”，而是已经有 3 条半成品链路：

1. parser 链已经在产出接近统一的结构化 JSON
2. office 链已经有 `framework_file_json_*` 的包 / 版本 / patch / task 底座
3. docs-open 已经能按文件类型把内容读成结构化 JSON，再尝试写回

当前问题：

- 各格式 JSON schema 仍然分叉
- `json_content` 只是存了 JSON，不等于平台级 `document_ir`
- agent 读写不同格式时仍要适配不同 payload
- knowledge / docs-open / office / parser / office-gen 还没共用一套文档对象
- 写回链路仍是逐格式 `if/elif`，不是统一 IR projection

### 参考后逻辑

- Docling：
  - `README.md:37`
  - `docs/reference/docling_document.md:3-26`
  - `tests/test_backend_docling_json.py:33-36`
  - 强在 `DoclingDocument` 作为统一文档对象，可序列化、可回载、跨格式一致
- Unstructured：
  - `unstructured/documents/elements.py:150, 424-449, 718-815, 952-1001`
  - `unstructured/staging/base.py:265`
  - 强在 `Element + ElementMetadata + to_dict()` 的轻量统一元素层
- Pandoc：
  - `changelog.md:1234-1240`
  - 强在“一套 AST / 一套 JSON contract / 多 reader-writer 投影”的纪律性

最强逻辑判断：

- Docling 在“多格式统一文档对象 + provenance + 表格/图片语义”上最强
- Unstructured 在“轻量元素归一化 + metadata/coordinates”上最强
- Pandoc 在“IR 先行、reader/writer 后置”的架构纪律上最强

### 谁最强，为什么

- Docling 最值得借的是：不要把统一层做成“文本块列表”，而要让表格、图片、来源、页面都成为文档对象的一部分
- Unstructured 最值得借的是：IR 不必一开始很重，先把 `element + metadata + to_dict()` 做稳，就足够支撑很多能力共用
- Pandoc 最值得借的是：一旦定义 IR，就让所有 reader/writer 服从它，而不是每个格式各搞一套 payload

### 这一步应该怎么改

第一刀不是重做所有 editor / viewer，而是把现有半统一链路收口成平台级 `Document IR`：

1. `Document IR`
2. `Resource IR`
3. `Patch IR`
4. `Projection IR`

先做“统一读 + 统一存 + 最小统一写”，不要一口气把所有格式都做成双向无损。

### 必改文件

- `backend/app/models/office.py`
- `backend/app/services/office/json_package_service.py`
- `backend/app/routers/office.py`
- `backend/app/routers/office_export.py`
- `modules/docs-open/backend/router.py`
- `modules/docs-open/backend/handlers/content.py`
- `modules/office-gen/backend/router.py`
- `modules/text-parser/backend/router.py`
- `modules/docx-parser/backend/router.py`
- `modules/xlsx-parser/backend/router.py`

### 提精度手柄

- 是否保留坐标 / bbox
- 是否保留表格结构化单元格，而不只保留拼接文本
- 图片是否统一落 `resource_ref`
- 是否保留 raw payload + normalized IR 双存
- projection 是按格式族生成，还是按单格式写死
- agent 是否只允许读取 IR，不直接读取 parser 私格式
- patch lineage 是否保留版本链

### 先改哪几段函数

1. `backend/app/services/office/json_package_service.py:24-67`
   - `create_package()` 先从空 `{manifest, content}` 壳升级成最小 `document_ir`
2. `backend/app/services/office/json_package_service.py:69-93`
   - `read_package()` 返回统一 `document_ir` contract，而不是裸 `json_content`
3. `modules/docs-open/backend/handlers/content.py:16-87`
   - `_read_content()` 把 txt/docx/pdf/xlsx/csv 收口到一套 IR
4. `modules/docs-open/backend/handlers/content.py:90-132`
   - `_write_content()` 从逐格式分支升级成 `IR -> projection -> writer`
5. `backend/app/routers/office.py:51-169`
   - package/version/patch/rollback 路由语义从 office json 包升级成 document ir contract
6. `backend/app/routers/office_export.py:38-113`
   - export 路由改成读取统一 IR，再投影回目标格式
7. `modules/text-parser/backend/router.py:35-137`
8. `modules/docx-parser/backend/router.py:35-101`
9. `modules/xlsx-parser/backend/router.py:35-108`
   - 这些 parser 先统一字段语义，不追求一次做完所有格式

### 验收

- 至少 4 种格式（txt/docx/pdf/xlsx）能通过同一 IR 读取
- 至少 2 种格式（docx/xlsx 或 docx/pptx）能通过同一 IR 回写
- `framework_file_json_*` 不再只是 office 专用概念
- knowledge / docs-open / office-gen / agent 能共用同一 `document_ir`
- 版本 / patch / rollback 仍可工作，不因 IR 升级退化

## A12 多人权限与分享协作控制面

### 当前 V2 逻辑

- `backend/app/middleware/auth.py:31-44`
  - `require_permission()`
- `backend/app/services/role_service.py:9-58`
  - 角色矩阵当前只有 `admin/editor/viewer`
- `backend/app/models/file_share.py:7-17`
  - `framework_file_shares`
- `backend/app/services/file_share_service.py:9-28`
  - `check_file_access()`
- `backend/app/services/file_share_service.py:31-79`
  - `create_share()`
- `backend/app/services/file_share_service.py:93-176`
  - 收件 / 发件分享列表
- `backend/app/services/file_service.py:164-177`
  - 框架级 `check_file_access()`
- `backend/app/routers/file_shares.py:19-82`
  - share CRUD / list / check
- `backend/app/routers/editors.py:18-40`
  - read/write 权限依赖 owner 或 shared `edit`
- `backend/app/routers/office.py:18-27, 62-69`
  - office package 访问也复用 file access

当前底座其实不差：

- JWT + session_version 有了
- 角色层级门槛有了
- file owner/share/read/edit 有了
- `check_file_access()` 已经是统一文件边界
- office / editor / docs-open 都在复用文件访问链

当前问题：

- 权限还是 file 级二维模型
- 分享目标只有 `owner -> user`
- 权限只有 `read/edit`
- 没有 share expiry / reason / audit / target_type / scope
- 没有 document IR / artifact / evidence 级权限语义
- 没有“可读但不可编辑”“可协作但不可发布”“可引用但不可转发”这种协作语义

### 参考后逻辑

- OpenClaw：
  - control plane / contract / governance 思维更成熟
- Claude：
  - 权限边界、默认审批意识、执行前约束更硬

最强逻辑判断：

- OpenClaw 在协作控制面和资源治理抽象上更强
- Claude 在默认边界和可执行约束落地上更强

### 谁最强，为什么

- OpenClaw 最值得借的是：把权限当作 control plane，不是几个 if 判断
- Claude 最值得借的是：先把边界写死，再开放能力，不让运行时靠隐含约定碰运气

### 这一步应该怎么改

不要一口气上组织架构系统，先做“协作控制面最小版”：

1. role policy
2. share policy
3. resource permission resolver
4. audit / expiry / approval
5. file / document_ir / artifact / evidence 一致口径

### 必改文件

- `backend/app/models/file_share.py`
- `backend/app/services/file_share_service.py`
- `backend/app/routers/file_shares.py`
- `backend/app/services/file_service.py`
- `backend/app/services/role_service.py`
- `backend/app/routers/roles.py`
- `backend/app/middleware/auth.py`
- `backend/app/routers/editors.py`
- `backend/app/routers/office.py`
- `modules/docs-open/backend/router.py`

### 提精度手柄

- 分享目标是 user / group / space 哪一种
- 权限是 read / comment / edit / publish / reshare 哪一种
- 是否带有效期
- 是否必须审批
- 是否允许再分享
- 文档 IR 是否可协作但不可发布
- evidence package 是否可引用但不可改写源文件

### 先改哪几段函数

1. `backend/app/models/file_share.py:7-17`
   - 先把模型从 `user + read/edit` 扩成可表达 scope / expiry / audit 的最小版
2. `backend/app/services/file_share_service.py:31-79`
   - `create_share()` 从 owner->user 单文件分享升级成更可治理的 grant 逻辑
3. `backend/app/services/file_share_service.py:9-28`
   - `check_file_access()` 或其上层升级成 resource permission resolver
4. `backend/app/services/role_service.py:9-58`
   - 角色矩阵增加协作相关权限位
5. `backend/app/middleware/auth.py:31-44`
   - `require_permission()` 不再只靠粗粒度角色层级
6. `backend/app/routers/file_shares.py:19-82`
   - share API 返回有效权限解释，而不只回 share 行
7. `backend/app/routers/editors.py:29-40`
   - write 权限判定升级成可解释协作权限
8. `backend/app/routers/office.py:18-27, 62-69`
   - package / version / patch 访问也统一走新的资源权限链

### 验收

- 保留原有 owner/share 链路，不退化
- 分享模型至少支持 expiry / reason / scope 中的最小版本
- 角色矩阵可表达协作能力位
- `file`、`document_ir`、`artifact`、`evidence` 至少有同口径权限解释
- 同事协作场景能明确区分：我能看、我能改、我能发布、我能再分享

## 如果只能先做 3 个点（01-06 已落地后）

剩余节点中优先顺序固定为：

1. `backend/app/services/asset_service.py:25-192` + `modules/agent/backend/runtime/task_sink.py`
   - A8 收尾：已开一半，收尾代价最低。把 tool 输出自动落 artifact 做掉
2. `modules/agent/backend/engine/engine.py:299-308` + `dev_toolkit/server.py:971`
   - A10 第一刀：默认工作流强吸入 + probe→memory_write 联动
3. `backend/app/models/file_share.py:7-17` + `backend/app/services/file_share_service.py:31-79`
   - A12 第一刀：分享模型升 expiry/scope/audit

这三个点不顺，A11 文档 IR 做出来后也接不上资产/权限/工作流。

## 这一轮最该注意的三块（01-06 已落地后）

### 1. A8 文件→资产系统已开一半，必须收尾

`asset_service.py` + `FileAsset` 模型已落，但 agent 侧 tool 输出还没自动挂 artifact。  
如果不收这步，A11 文档 IR 发布、A12 协作共享都缺语义统一的“资产”入口。

必须做的：
- `modules/agent/backend/runtime/tool_loop_runtime.py:65` — tool 循环结束后调 `sink.record_assets()`
- `backend/app/services/file_service.py:125` — `delete_to_trash()` 感知 asset_type

### 2. 知识库证据面已落，但 multi-hop / grading 未接入 runtime

A4 已有 EvidencePacket / answerability / rewrite，但 agent 侧调用仍偏简单（调一次 search 就结束）。  
需要把 multi-hop evidence planning 接入知识库 capability。

### 3. A10 和 A12 的改动互相独立，可并行开工

A10（工具台工作流）不依赖 A12（权限协作），A12 也不依赖 A10。  
这两个在 W1 批次里可以同时派两个 agent 开工。

## 最后一句话

01-06 已经把 V2 从“能跑的 agent 模块”升级成“有骨架的 agent 平台”。  
剩余 A8/A10/A11/A12 是把这个骨架的平台化程度继续做深。
