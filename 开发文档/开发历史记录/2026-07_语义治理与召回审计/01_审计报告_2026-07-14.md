# V2 项目全量审计报告
**审计时间**：2026-07-14
**审计方式**：直读代码、打探针验证实际运行状态

---

## 一、已修复问题清单

### 1. ✅ 知识库 provider 拆分（本地 vs 云端）

**改动**：`backend/data/config/models.json` + `backend/.env`

- 新增 `gptstore-local` provider（50936 端口本地代理，密钥 GPTSTORE_LOCAL_API_KEY）
- 知识库文本分析（gpt-5.5-knowledge）改走本地 50936，零费用
- Agent 视觉/生图保留走 jayce 中转站（gptstore-text）
- image_gen 的 gptstore profile 改用 jayce 而非 opencode（deepseek）

### 2. ✅ fallback chain 补全

**改动**：`backend/data/config/models.json`

- `knowledge_text_primary`: chain → ["gemma-4", "ollama-local"]
- `knowledge_vision_primary`: chain → ["qwen3-vl"]
- gptstore 挂掉时有降级路径

### 3. ✅ context_budget 配置

**改动**：`backend/data/config/models.json`

- deepseek-v4-flash: null → 1000000（100 万 token）
- deepseek-v4-pro: null → 1000000

### 4. ✅ 端口统一

**改动**：`backend/app/config.py` + `backend/.env` + `dev_toolkit/diagnose_tools.py`

- config.py 默认端口 30004 → 33000（与实际启动一致）
- .env 显式写 APP_PORT=33000
- diagnose_tools.py 从 config_loader 动态读端口

### 5. ✅ DB 连接池 vs executor 数量对齐

**改动**：`backend/app/config.py` + `backend/data/config/task_worker.json`

- DB_POOL_SIZE: 5 → 20
- DB_MAX_OVERFLOW: 1 → 10
- max_executors: 256 → 32

### 6. ✅ hello-world 模块删除

**改动**：删除 `modules/hello-world/` 整个目录

### 7. ✅ pipeline_concurrency JSON 格式修复

**改动**：`backend/data/config/models.json`（model_call_global 缩进错位）

### 8. ✅ agent_code 硬编码改造

**改动**：
- `modules/agent/backend/schemas.py` — ChatRequest 加 agent_code 字段
- `modules/agent/backend/runtime/conversation_runtime.py` — 从 payload 读取并传递
- `modules/agent/backend/runtime/tool_loop_runtime.py` — __init__ 接收 agent_code，内部使用 self.agent_code
- 所有 assemble_context / thinking_router 调用点传入实际 agent_code
- 默认值保持 "erp_chat"，完全向后兼容

### 9. ✅ 向量双轨清理

**改动**：`backend/data/config/models.json`

- embedding.primary: "bge-m3" → "qwen3-embedding-8b"
- bge-m3 profile 加 `deprecated: true`
- kb_chunks vector_store: migration_status → "deprecated_read_only"
- 新嵌入全走 qwen3-embedding-8b (4096D, 30004 端口)

---

## 二、保留未动的（你确认过的合理状态）

| 项目 | 状态 | 原因 |
|------|------|------|
| 知识库 paused_stages | 保留暂停 | 额度没恢复，暂不跑 LLM/VLM |
| MIMO key 缺失 | 保留空 | 订阅未购买，用 jayce gpt-5.5 看图 |
| 422 个失败任务 | 不清理 | 数据问题（外盘断开/文件损坏），非代码 bug |

---

## 三、遗留事项（后续可做）

| 项目 | 说明 |
|------|------|
| profile_vector_service.py | 还在用 bge-m3 (1024D) 做文档 profile 向量，graph/relations 阶段暂停着不影响，解除暂停后需要迁移 |
| 前端 useAgentChat 拆分 | composable 过胖，建议拆 useSSEStream / useWorkGroup / useMessageEdit |
| kb_enterprise_import_file 340 个 | source_root 路径不存在，需要确认那批外盘是否还要接回来 |
| conversation_id=0 的后台任务 | owner_id=1 的测试用户触发了无效任务，可以直接清 |

---

## 四、当前系统端口分配

| 端口 | 服务 | 状态 |
|------|------|------|
| 33000 | 后端 FastAPI | ✅ 运行中 |
| 5173 | 前端 Vite | 开发时启动 |
| 30000 | bge-m3 嵌入 (1024D) | deprecated |
| 30001 | bge-reranker | ✅ |
| 30002 | qwen3-vl 视觉 | 按需启动 |
| 30003 | gemma-4 文本 | 按需启动 |
| 30004 | qwen3-embedding-8b (4096D) | ✅ 运行中 |
| 50936 | 本地 gpt-5.5 代理 | ✅ 运行中 |

---

## 五、模型路由总结

```
Agent 对话 → deepseek-v4-flash (opencode 订阅)
Agent 视觉/看图 → gpt-5.5 via jayce 中转站
Agent 生图 → gpt-5.5 via jayce 中转站
知识库文本分析 → gpt-5.5 via 本地 50936 → fallback gemma-4 → ollama
知识库视觉分析 → gpt-5.5 via jayce → fallback qwen3-vl 本地
向量检索 → qwen3-embedding-8b (4096D, 30004 端口)
重排序 → bge-reranker (30001 端口)
```
