# 方案07 WP4–WP6 施工记录

日期：2026-07-17
承接：`12_方案07_WP4-WP7接手交付文档_20260717.md`
边界：**做到 WP6 收口即停，WP7 不许碰**

## 本轮目标

1. WP4：Product Catalog + Content Open Resolver + 首批 10 Product
2. WP5：Office/Content Runtime 写骨架（draft/save/lease/hydrate）
3. WP6：Desktop State CAS + product-runtime/platform-sdk/workspace-runtime 前端入口

## 不做

- WP7 读者翻转 / 存量 canonical 回填 / finalize 约束切换
- 下线 knowledge `file.uploaded` 双入口
- 假绿标 ready

## 落点文件（预期）

见交付文档 12 §三~§五 + 本记录「实际结果」。


## 实际结果

### 完成
- WP4 Catalog/Resolver/10 products + API
- WP5 draft/save/lease/office home
- WP6 desktop CAS + product-runtime/platform-sdk/workspace-runtime
- 测试 7+26 相关通过；真实 probe 通过
- **WP7 未碰**

### 验证命令
```bash
cd frontend && node scripts/scan-products.js
cd backend && JWT_SECRET=test-secret .venv/bin/python -m pytest -q tests/test_wp4_product_catalog.py
# probe: GET /api/desktop/products, POST /api/content/drafts, POST /api/content/open, POST /api/content/packages/{id}/save
```

## 加速落盘补完（同日续）

按华哥要求：先把方案落到可交付，不做半截验收。

### 追加落盘
1. 桌面主路径 `app-loader` 读 `/api/desktop/products`
2. `app-opener` 打开文件走 Content Open Resolver（失败降级旧后缀）
3. `scan-products` + `@products` 组件映射，Office/内容工作室可加载
4. Desktop State CAS 前端接线（`expected_version` + 冲突重载）
5. Office / Content Studio 产品壳可点
6. 启动器/桌面按 product sortOrder 排序

### 边界
- WP7 未碰
- 完整 Office 排版编辑器未做（产品壳+Runtime 会话）
- 旧 apps/后缀 registry 仅作兼容降级

### 编译
- `node scripts/scan-products.js` 10 products / 2 product components
- `vue-tsc -b` 通过

## 非WP7继续补满（同日）

边界：WP7 数据库回填/约束 finalize/生产数据改写不动；其余可动。

### 追加
1. `content_access_policy.py` 统一 package 读写判定（owner/分享）
2. content_open 接线 export/publish + 读/写权限门
3. 桌面 loader：products 成功后 **只展示 Product**（旧 apps 仅 catalog 失败兜底）
4. 文件关联排序优先 office/text/media/files，压低 *-viewer
5. Office 壳：导出/发布按钮 + API 客户端
6. platform-sdk 导出 export/publish

### 仍属 WP7 未做
- 存量 canonical 回填
- 翻转 Knowledge/Viewer 读者
- finalize 唯一/非空约束
- knowledge file.uploaded 下线

## 单路径收口（华哥：去掉 WP7/操库，不要两套）

- 桌面只 products
- 打开只 Resolver
- 文档改为正式单路径交付说明：`12_方案07正式交付说明_单路径_20260717.md`
- 不再维护 WP7 操库计划
